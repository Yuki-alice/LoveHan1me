# 阶段一 · 功能对齐 —— 进度报告

> 日期：2026-09-10 ｜ 分支：master
> 相关提交：`cc8d768` → `b31fbc1` → `edf42b3` → `c5c28fc`
> 上游清单：`阶段一_功能对齐清单.md`

---

## 0. 一句话结论

**编译战打赢了。** `E:\LoveHan1me` 首次在 Windows 上通过编译，四个可构建目标全绿；
E3 包结构重组留下的 284 个编译错误已定位并修复（含 1459 处 import 中被改坏的部分），
并新增了一把可复用的静态校验工具。移除项 ③⑫ 已完成，⑪⑥ 因技术约束需你先拍板。

---

## 1. 编译验证结果

| 目标 | 任务 | 结果 |
|---|---|---|
| shared · 桌面端 | `:shared:compileKotlinDesktop` | ✅ 0 错误 |
| shared · Android | `:shared:compileAndroidMain` | ✅ 0 错误 |
| app · Android 应用 | `:app:compileDebugKotlin` | ✅ 0 错误 |
| desktopApp | `:desktopApp:compileKotlin` | ✅ 0 错误 |
| shared · iOS | `compileKotlinIosArm64` | ⏸ Windows 无法编译（Kotlin/Native 仅 macOS 宿主），改用静态校验兜底 |

**iOS 兜底手段**：`fix_e3_imports2.py` 静态校验全仓 import（含 iosMain），
当前结果 **0 处待修 / 0 处待人工**。它不能替代真机编译，但能覆盖 E3 最主要的一类损坏。

回归命令（每次改完跑一遍，约 2 分钟）：

```bash
cd /e/LoveHan1me
GRADLE_USER_HOME=D:/DevCache/.gradle ./gradlew \
  :shared:compileKotlinDesktop :shared:compileAndroidMain \
  :app:compileDebugKotlin :desktopApp:compileKotlin --console=plain
```

> 注意：本机 Android SDK 只有 `platforms/android-36`（无 cmdline-tools 补装），
> 故 `gradle.properties` 的 `han1me.android.compileSdk` 从 37 降到 **36**。
> 你 Mac 上若装了 37 可以改回去，两边不影响源码。

---

## 2. E3 后遗症：根因与修复

### 2.1 根因（已用 git 历史坐实）

E3 收尾提交 `0361c81`（"顶层散件归位、viewmodel 分摊到 feature"）的脚本
按「符号名 → 所在包」重写 import，踩了两个坑：

**坑一：把资源访问器的包名改错**

Compose Resources 生成的访问器是这样的：

```kotlin
// shared/build/generated/compose/resourceGenerator/kotlin/commonMainResourceAccessors/lovehan1me/String0.commonMain.kt
package lovehan1me
public val Res.string.about: StringResource by lazy { ... }
```

即「**包 `lovehan1me` 下的扩展属性**」，`Res.string.` 是接收者类型、不进 import 路径。
所以正确写法是 `import lovehan1me.about`，而脚本改写成了：

| 坏形态 | 正确形态 | 处数 |
|---|---|---|
| `import lovehan1me.desktop.loading` | `import lovehan1me.loading` | 6 |
| `import lovehan1me.app.navigation.main.cancel` | `import lovehan1me.cancel` | 45 |
| `import lovehan1me.data.network.close` | `import lovehan1me.close` | … |
| `import lovehan1me.core.domain.model.mmd` | `import lovehan1me.mmd` | … |

**坑二：把访问器 import 整行删掉**
用法 `Res.string.X` 还在，import 没了（如 `App.kt` 的 5 个 `app_source_*`）。

### 2.2 另一类：`HanimeAccount.kt` 换包引发的同名冲突

`HanimeAccount.kt` 原先在**扁平包 `lovehan1me`** 下（`login()` / `logout()` 函数），
E3 把它挪进了 `lovehan1me.data`。而 `login` / `logout` 同时又是资源名，
于是 `import lovehan1me.login` 不再能同时提供「访问器 + 函数」。

修法：函数侧改别名。

```kotlin
import lovehan1me.login                                  // 资源访问器（界面文案）
import lovehan1me.data.login as persistLogin             // 写会话的函数
...
persistLogin(state.info)
```

涉及 `FormLoginScreen.kt`、`AuthRouteScreens.kt`（`AccountScreen.kt` 该函数未被调用，直接换成访问器）。

### 2.3 工具（可复用）

`.workbuddy/fix_e3_imports2.py` —— 不依赖编译日志的全仓静态校验器，三道工序：

1. **校验 import**：取最长「真实存在的包」前缀，剩余段能否解析为声明；解析不了就按最后一段重修
2. **补编译日志点出的缺失 import**（`--log <编译日志>`）
3. **扫 `Res.string.X` / `Res.drawable.X` 用法**，补齐被删掉的访问器 import；同名冲突则报人工

```bash
python .workbuddy/fix_e3_imports2.py --log .workbuddy/build-logs/errors-1.txt        # 干跑
python .workbuddy/fix_e3_imports2.py --log .workbuddy/build-logs/errors-1.txt --apply
```

**⚠️ 踩过的坑**：脚本写盘必须 `newline=""`。仓库全仓 LF，
Python 默认会把 `\n` 翻成 `\r\n`，第一次跑出 3.8 万行假 diff。已加 `.gitattributes` 锁定 `eol=lf`。

---

## 3. 移除项执行情况

| # | 项目 | 状态 | 说明 |
|---|---|---|---|
| ③ | 生物识别 | ✅ 完成 | **连带应用锁功能整体删除**（该功能唯一机制就是 BiometricPrompt，桌面/iOS 无对应能力，留开关即死配置）。动了 `MainActivity`/`AndroidShell`/`AppSettings`/`DataStoreManager`/`HomeSettings{UiState,Screen,Route}`/`HomePlatformActions`，零残留 |
| ⑫ | Glance 小组件 | ✅ 完成 | 删 Provider + `checkin_widget_info.xml`（含 v31 变体）+ Manifest receiver + `CheckInWidgetUpdate` 三端壳 + 5 处调用 + glance 依赖 |
| ① | NDK `cpp/` | ✅ 完成 | 早前已删 |
| ⑪ | 投屏 Cast | ✅ 完成（选项 A） | **耦合比预估深，改了 25 个文件**：除引擎/Provider/依赖/Manifest 外，还牵连 `PlaybackEngine` 的 3 个状态字段、`VideoPlatform.CastRouteButton`（MediaRouteButton 是 media3-cast 的传递依赖）、`PlaybackEngineFactory.allowCast`（expect + 3 端）、播放器设置页的投屏设置组、播放器 UI 的投屏按钮与「正在投屏到 X」提示 |
| ⑥ | 下载通知 | ✅ 完成（安全子集） | 删 4 类**结果**通知 + 死代码 `UPDATE_NOTIFICATION_CHANNEL`；**保留进度通知**，理由见下 |
| ⑨ | 桌面端 PiP | ✅ 已满足 | 实测**本来就没实现**（`shouldEnterPip()` 恒 `false`），那几行是接口要求的覆写、不是死代码。已在站点补注释固化决策，不做无意义改动 |

### ⑥ 的落地偏差：为什么没有"全删"

`HanimeDownloadWorker` 里的进度通知同时是 **WorkManager 前台服务
（`FOREGROUND_SERVICE_TYPE_DATA_SYNC`）的载体**：

```kotlin
private suspend fun createForegroundInfo(progress: Int = 0): ForegroundInfo {
    val notification = createDownloadNotification(progress)
    return ForegroundInfo(downloadId, notification, ServiceInfo.FOREGROUND_SERVICE_TYPE_DATA_SYNC)
}
```

Android 14 上把它一起删掉，应用退到后台后下载会被系统杀掉。所以执行的是：

- **删**：完成 / 失败 / 重试 / 文件已存在 四类**结果通知**（22 处）
- **留**：进度通知（前台服务必需）
- 这四类结果提示本来就已经**同时**配了 `SonnerToast`（跨端共享的提示面），
  删掉通知后用户仍然能看到结果 → 相当于去掉了一层 Android 独有的重复提示

如果你要的是字面上的"连进度通知也不显示"，那需要改成"仅前台下载"或
`setExpedited`（有配额限制），后台下载可靠性会下降——需要你确认再动。

---

## 4. 补齐/攻坚项实测结论（重要修正）

### ⑩ Cloudflare 验证 —— **完成度远比清单里写的高**

`阶段一_功能对齐清单.md` 说「Desktop 显式 return null、iOS 无通路」，**这个判断是错的**。实测：

```
                ┌─ NetworkRepo.throwRequestException()
                │     403 + "Just a moment" → CloudflareChallenges.request(url)
                ▼
        CloudflareChallenges（commonMain 共享总线）
                │
        App() 收集 → 压栈 CloudflareRoute(url, host)   ← 三端共用，已实现
                │
        PlatformScreens.cloudflare 平台槽位
        ├── Android：OkHttp 拦截器 → CloudflareVerifier.launcher
        │            → :app CloudflareVerificationCoordinator → Activity 验证页
        ├── Desktop：CloudflareVerificationWindow（KCEF/Chromium 内嵌窗 + 轮询 cf_* cookie
        │            写回 HCookieJar，UA 与 HTTP 层对齐）
        └── iOS    ：CloudflareVerificationWebView（WKWebView）
```

关键事实：`createCloudflareInterceptor()` 的 expect 声明在 **jvmMain**（`data/network/NetworkPlatform.kt`），
只覆盖 desktop + android；**iOS 根本不走拦截器**，走的是共享总线 → 所以 iOS 不需要 actual，不存在缺失。

**⑩ 的落地（本轮已完成关键一环）**：

原实现的问题：`CloudflareKcef.ensureInit()` 把所有失败都吞进 `runCatching{}.onFailure{ printStackTrace() }`，
窗口里的 WebView 永远加载不出来，**用户只看到一个卡死的窗口 = 桌面端打不开应用**。

现在改成：

| 状态 | UI |
|---|---|
| `Initializing` | 「正在准备验证组件……首次使用需下载约 200MB 的 Chromium 运行时」+ 「改用浏览器手动完成」入口 |
| `Ready` | 原来的 KCEF WebView（轮询 `cf_*` cookie 自动判定） |
| `Failed` / 超时(120s) | 手动兜底面板：① 「用系统浏览器打开验证页」→ `Desktop.browse(URI(url))`；② 粘贴 `cf_clearance` → 写回 DataStore；③ 「重试自动验证」 |

关键实现点：

- **超时只是 UI 闸门**：`KCEF.initBlocking` 是阻塞调用，`withTimeout` 打断不了它。
  所以超时只把 UI 切到兜底面板，**后台初始化继续跑，一旦成功会自动切回 WebView**。
- **手动 Cookie 只需写 DataStore**：`HCookieJar.loadForRequest` 在
  `cloudFlareCookieHost == host` 时会叠加 `cloudFlareCookie`，后续请求自动携带。
  （顺带发现：`saveFromResponse` 每次响应都会覆盖 `cookieMap[host]`，所以写内存 map 本来也不可靠。）
- 新增 6 个文案键 × 三语言，走项目「字符串单源」约定，未硬编码。

**仍未解决 / 未验证**：

- Android 拦截器与共享总线的去重（"栈上已有 CF 页"逻辑）**未实测**。
- iOS 通路完全未经真机验证（Windows 编译不了 iosMain）。
- KCEF 失败场景本身未实测（要真实触发一次下载失败）——逻辑上是通的，但没有端到端验证。

### ② 视频超分：**Desktop 已完成**（本轮）

| 端 | 状态 |
|---|---|
| Desktop（mpv） | ✅ **本轮从 0 到 1** |
| Android（mpv 内核） | ✅ 本来就有，本轮改为共享 shader 路径 + 失败不抛异常 |
| Android（ExoPlayer 内核） | ⏳ 需补 Media3 `GlEffect` + `BaseGlShaderProgram`（1–2 天） |
| iOS | ❌ 不做（AVPlayer 不支持挂自定义 shader） |

**Desktop 实现要点**

1. **取 mpv 句柄**：mediamp 把它标成 internal，JVM 名被 mangled 成
   `getHandle$mediamp_mpv`。Kotlin 反引号转义不了 `$`，所以用反射
   （`javaClass.getMethod(...).invoke(...)`）——失败只返回 null，随后降级 OFF，不会崩。
2. **挂 shader**：`MPVHandle.command("change-list", "glsl-shaders", "set", paths)`
3. **同步缩放属性**：`scale`/`cscale`/`dscale`/`sigmoid-upscaling`（animeko 同款），
   首次切档记住原始值，OFF 时精确还原。
4. **补上 animeko 的两个短板**：
   - 失败**自动降级** `QUALITY → PERFORMANCE → OFF`（animeko 会直接抛
     `VideoFrameProcessingException`）
   - OFF 时还原**原始**属性，而不是写死默认值
5. **⚠️ 路径分隔符**用 `File.pathSeparator`：mpv 的路径列表在 Windows 用 `;`、Unix 用 `:`。
   写死 `:` 的话，`C:\...` 里的冒号会和分隔符打架。

**Shader 资产三端统一**（顺手消掉一份重复）

`androidMain/assets/shaders/` → `commonMain/composeResources/files/shaders/`，
新增 `MpvShaders`（档位定义 + `expect materializeMpvShaders/mpvShaderTargetDir`）：

- jvmMain 做落盘（android + desktop 共享，走 `readAssetBytes`）
- androidMain → `filesDir/shaders`；desktopMain → `~/.lovehan1me/shaders`；iosMain → null
- Android 的 `AnimeShaders` 只留 CA 证书逻辑；`setSuperResolution` 改为
  「失败即放弃挂 shader」，不再抛 `IllegalStateException`

**顺手修掉一个 UI 门控 bug**

原来用「内核名字 == `MpvPlayer`」判断是否显示超分入口。但桌面端引擎**恒为 mpv**，
而用户设置里的 `switchPlayerKernel` 可能是 ExoPlayer（Android 默认值）
→ **桌面端永远看不到超分入口**。已改为引擎能力判断：
`PlaybackEngine.supportsSuperResolution()`（mpv 系引擎返回 true）。

**未验证**：shader 实际渲染效果与性能（要真机跑起来看），只做了编译与逻辑验证。

### ④ 备份/恢复：**数据层已下沉，UI 层与 iOS 文件选择器仍待做**

**已完成（本轮）**：`BackupManager` 与 `OnlineListsBackup` 从 `jvmMain` 搬进 `commonMain`。

`BackupManager` 卡在 jvmMain 的真正原因只有两个，都已解决：
1. `java.io.OutputStream` → 改成 okio 直写 `BufferedSink.writeUtf8()`
   （`openBackupSink/Source` 的签名本来就是 `okio.Sink?` / `okio.Source?`，顺手显式声明了 okio 3.18.1，
   之前靠 ktor 传递依赖）
2. `HProxySelector.rebuildNetwork()`（只设置 JVM 系统属性 proxySet/proxyHost/proxyPort）
   → 新增 `ProxyPlatform`：`expect rebuildSystemProxy()`，
   **actual 放在 jvmMain 同时服务 android + desktop**（走项目已有的 jvm 中间源集），iosMain 为 no-op
   （iOS 本来就没有系统代理能力，设置页已明示 "Custom DNS / proxy is not available on iOS"）

**剩下的两层（都不是"移动文件"能解决的）**：

| 层 | 现状 | 阻塞点 |
|---|---|---|
| UI 层 | 备份设置页在 `jvmMain/HomeSettingsRoute.kt`（1376 行），**iOS 根本走不到** | 需先确认它依赖的 jvmMain API 是否都已 expect 化，再整体搬 commonMain |
| iOS 文件选择器 | `openBackupSink/Source`、`writeBackupText/readBackupText`、`rememberBackup*Launcher` 全是 no-op | **⚠️ 有个反直觉的 API 约束，见下** |

> **iOS 导出的关键约束（本轮实测到，避免你重复踩）**：
> 现在的流程是「先选目标位置 → 再写内容」（Android SAF / 桌面文件对话框都是这样）。
> 但 iOS 的 `UIDocumentPickerViewController(forExporting:)` **要求先有一个已存在的文件**，
> 由系统把它搬到用户选的位置——也就是「先写临时文件 → 再导出」。
> 两者流程相反，所以 iOS 侧要么在共享层把导出拆成「写临时文件」+「导出」两步，
> 要么 iOS 改走「保存到 App 自己的 Documents 目录」这种语义。
> **这是设计选择，需要你拍板**，我不能替你决定用户体验。

### ⑧ 头像裁剪：需要自研纯 Compose 裁剪器

`app/.../AvatarCropScreen.kt` 深度依赖 Android：`android.graphics.Bitmap` / `ImageDecoder` /
`asAndroidBitmap()` / `LocalContext` / `java.io.File`。
更关键的是它用的 `cn.mucute:compose-avatar-cropper` **只有 `-android` 产物**（缓存里
`compose-avatar-cropper-android` 之外没有任何 ios/jvm 变体），是 Android-only 库。

所以 ⑧ 不是"下沉"，是**重写**：纯 Compose 的裁剪交互（缩放/平移/裁剪框）+ 平台图片解码/编码 expect/actual。
清单里我之前写"纯 Compose + 平台 ImageDecoder"就是这个意思，但工作量比"下沉"大得多（半天到一天）。

### ⑦ `importDownloaded()`：**桌面端已完成**（本轮）

桌面端此前恒 `false`——下载页的「导入已下载」按钮点了没反应。现在按 `addTask` 的落盘约定
扫描 `<dir>/<videoCode>/<title> [<quality>].<suffix>`：

- 桌面端**不写 info.json**（Android 靠 SAF 扫描 + info.json 还原元信息），
  所以从**目录名/文件名反解** videoCode、标题、清晰度
- `videoUri` 存绝对路径：实体里的 `suffix` 由 `videoUri` 反解得到，
  于是 `videoFile(entity)` 能重新算出同一个文件，**回放路径自洽**
- 已在库的（videoCode + quality）跳过，可重复调用；无命中返回 false
- **已知局限**：封面等元信息只能走占位（文件名里没有封面信息）

iOS 仍恒 `false`：沙盒下没有可自由扫描的共享下载目录，需要 DocumentPicker 选目录——
和 ④ 的 iOS 导出是**同一类设计问题**，等你对导出流程拍板后一起处理。

### ⑤：未开始

iOS 网络设置页（Darwin engine 配置子集）。

---

## 5. 两个决策点的处理结果（已按 A 方案执行）

| 决策点 | 结论 | 备注 |
|---|---|---|
| ⑪ Cast 移除范围 | **A：彻底删** | 连播放器设置页的投屏设置组一起删。实测耦合面比预估大，见 §3 |
| ⑥ 下载通知 | **A：只删结果类** | 进度通知因前台服务硬约束保留，见 §3 说明；如需字面全删请再确认 |

> 原提出的两个选项供留档：Cast 的 B 方案是「保留 `CastAvailability` 恒假桩 + 设置组自动隐藏」，
> 下载通知的 B 方案是「连前台通知一起删，改走 `setExpedited`」。均未采用。

---

## 6. 建议的下一步顺序

**已完成**：编译战 ✅ · E3 后遗症 ✅ · 移除项 ①③⑥⑨⑪⑫ ✅ · ⑩ 桌面自救降级 ✅
· ④ 备份数据层 ✅ · ② 视频超分 Desktop ✅ · ⑦ 导入已下载（桌面）✅

**待办**（按建议优先级）：

1. **（你）在 Mac 上跑一次 iOS 编译** —— 仍是唯一的能力空白，越早越好。
   命令：`./gradlew :shared:compileKotlinIosArm64`
2. **（你）拍板 iOS 备份导出的流程设计**（见 §4 的 UIDocumentPicker 约束），
   我再把 ④ 的 UI 层与 iOS 选择器补完
3. **② 视频超分收尾**：~~Desktop mpv~~ 已完成 → Android ExoPlayer（Media3 `GlEffect`，1–2 天）
   + **实际渲染效果与性能验证**（需真机跑，本轮只做了编译/逻辑验证）
4. **⑧ 头像裁剪**（需自研纯 Compose 裁剪器，半天到一天）
5. **⑤ iOS 网络设置页**（Darwin engine 配置子集）；⑦ 的 iOS 侧随 ④ 一起定
6. **⑩ 收尾**：Android 拦截器与共享总线的去重实测；KCEF 失败场景端到端验证
7. **零散清理**：⑥ 留下的 6 个孤立字符串键（`download_task_completed` 等，三语言各一份）

---

## 6.5 环境坑（本轮新踩）

**`classes.jar` 被占用导致构建假失败**：
```
Execution failed for task ':shared:bundleAndroidMainClassesToCompileJar'.
> java.nio.file.FileSystemException: ...\classes.jar: 另一个程序正在使用此文件
```
不是代码错误。持有者是 **Kotlin 编译守护进程**（命令行含 `-cp D:\DevCache\.gradle\caches\modules-2\...`）。
解法：杀掉 `*DevCache\.gradle\jdks*` / `*modules-2*` 的 java 进程后重试即可。
（另：废弃项目 `E:\hanime1` 的残留 java 进程又会自己起来——本轮又清掉 10 个，
它们是 `gradlew` wrapper + daemon + 那个没用的 Compose Hot Reload watcher。
若构建莫名报锁，先按 `E:\hanime1` 匹配清一遍。）

---

## 7. 本轮改动文件清单（便于 review）

```
.gitattributes                                  (新增) 全仓 LF 锁定
.workbuddy/fix_e3_imports2.py                   (新增) import 静态校验/修复器
gradle.properties                               compileSdk 37 → 36
app/build.gradle.kts                            卸 glance 依赖
gradle/libs.versions.toml                       卸 glance
app/src/main/AndroidManifest.xml                摘 widget receiver
app/.../ui/activity/MainActivity.kt             删生物识别 + 应用锁门控
app/.../app/main/AndroidShell.kt                删应用锁遮罩
app/.../app/navigation/main/AuthRouteScreens.kt login 别名
app/.../HCacheManager.kt                        补 HJson import
app/.../ui/widget/CheckInWidgetProvider.kt      (删除)
app/src/main/res/xml/checkin_widget_info.xml    (删除，含 xml-v31 变体)
shared/.../feature/home/CheckInWidgetUpdate.kt  (删除，含三端 actual 共 4 文件)
shared/.../core/domain/model/AppSettings.kt     删 useLockScreen
shared/.../data/datastore/DataStoreManager.kt   删 useLockScreen
shared/.../feature/settings/HomeSettingsScreen.kt        删应用锁开关
shared/.../feature/settings/model/HomeSettingsUiState.kt 删 useLockScreen
shared/.../app/navigation/settings/HomeSettingsRoute.kt  删应用锁接线
shared/.../core/platform/HomePlatformActions.{kt,+3 端}  删 isDeviceSecure
shared/.../feature/login/FormLoginScreen.kt     login 别名
shared/.../feature/account/AccountScreen.kt     logout import 修正
shared/.../feature/video/VideoRouteActions.kt   删小组件调用
shared/.../data/BackupManager.kt                删小组件调用
shared/.../feature/home/CheckInCalendarViewModel.kt 删小组件刷新
+ 45 个文件共 59 处坏了包路径的资源访问器 import
```
