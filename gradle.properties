# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html
# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
org.gradle.jvmargs=-Xmx4096m -Dfile.encoding=UTF-8
# JDK 路径是机器相关的，不要提交到仓库（会弄坏另一台设备的构建）。
# 各自机器在 GRADLE_USER_HOME/gradle.properties 里配置 org.gradle.java.home。
# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. More details, visit
# http://www.gradle.org/docs/current/userguide/multi_project_builds.html#sec:decoupled_projects
# 构建性能三件套（对标 animeko：它同样开了 caching + configuration-cache）。
# 本仓 3 个模块但 :shared 有 393 个 commonMain 文件，缓存收益同样明显。
org.gradle.parallel=true
# 构建缓存：跨机器/跨分支复用 task 输出（首次构建后收益最大）
org.gradle.caching=true
# 配置缓存：跳过重复的配置阶段。⚠️ 若某个插件报 "configuration cache" 不兼容，
# 先用 `./gradlew <task> --no-configuration-cache` 绕过并在此注释回退原因，不要直接删。
org.gradle.configuration-cache=true
# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app"s APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true
# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official
# Enables namespacing of each library's R class so that its R class includes only the
# resources declared in the library itself and none from the library's dependencies,
# thereby reducing the size of the R class for that library
android.nonTransitiveRClass=true
android.disallowKotlinSourceSets=false

# —— KMP 约定插件 (build-logic/han1me-kmp-library) 的工程级配置 ——
# 全模块 compileSdk 统一到 android-37（:shared 与 :app 同一套平台，CI 只装 android-37）。
# ⚠️ 改这个值前先确认**两台机器**都装了对应平台，否则另一台构建直接失败。
han1me.android.compileSdk=37
han1me.android.minSdk=29

# `:app` 是普通 Android 应用模块、不走约定插件，SDK 版本同样集中到这里管理。
# compileSdk 必须留 37：仓里 26 个 AAR 依赖（Coil3 / androidx.lifecycle 2.11 / appcompat
# 1.7.1 / media3 等）声明 minCompileSdk≥37，:app 降到 36 会在 :app:checkDebugAarMetadata 直接失败。
# ⇒ 平台只能统一在 37，"单 android-36" 除非整体降级这些依赖，否则做不到（见 2026-09-20 G1-2）。
# targetSdk 降到 36 是**行为变更**（退出 Android 17 强制行为，如 BaseActivity 的 enableEdgeToEdge
# 边到边表现），与 compileSdk 解耦、不需要额外平台，但需真机/模拟器实测后再合入。
han1me.android.appCompileSdk=37
han1me.android.appTargetSdk=36
han1me.android.appMinSdk=29
han1me.namespace.base=lovehan1me
han1me.ios.enabled=true
# Windows 上 iOS 模拟器测试任务无法运行，抑制 "Native task is disabled" 警告
kotlin.native.ignoreDisabledTargets=true