# 定位

这份文档说清楚 LoveHan1me 是什么、为什么存在、边界在哪。读者是我自己，以及在这个仓库里干活的 agent。对外那份是 README。

它和 docs/decisions.md 的分工很简单：这里写判断，那里写取舍。取舍会变，判断很少变。要是一条取舍追溯不到这里的任何一句话，那它多半已经失效，该回来重新问一次。

关于上游和参照项目的外部事实不写在这里，见 docs/evidence/2026-09-26-上游Han1meViewer与参照animeko形态.md。

## 这是什么

LoveHan1me 是从 Han1meViewer 长出来的。

Han1meViewer 是个只做 Android 的 hanime1.me 客户端，上游还在维护，功能已经相当完整：浏览、搜索、播放、下载、评论、订阅、我的列表。我们做的事，是把它重做成一套 Kotlin Multiplatform + Compose Multiplatform 的代码，让同一个客户端同时跑在 Android、桌面和 iOS 上。

其中有两件事得分开讲。

一是代码血缘。本仓库是从 Han1meViewer 的代码库重构来的，GPLv3，归属写在 NOTICE 里。身份已经切开了：应用名、包名、更新渠道、关于页都不再指向它。但血缘和许可归属留着，不假装没这回事。

二是视频模块。跨平台的播放这块照 animeko 的思路做，不是把安卓那套硬搬过去——在 commonMain 里定义播放抽象，各端接自己最合适的内核。抽象层用 mediamp，Android 接 ExoPlayer，桌面接 mpv，iOS 接 AVKit。桌面坚持 mpv 是因为 Anime4K 真链和画面调节只有它给得了，这条已经拍板，别再拿它当缺陷重提。

所以这个项目的难点不在功能。上游已经把功能想清楚了。难点在于同一份体验怎么在三个端上都成立。

## 为什么值得做

Han1meViewer 再好，它只在安卓上。换到桌面或者 iOS，就只能回网页去。

反过来说：这个项目的目标不是做一个功能更多的客户端，而是让同一个客户端在三个端上都存在。功能清单比不比得过长不是重点，先把端补齐。

## 和上游的关系

Han1meViewer 是上游，不是对手。代码从它来，GPLv3 的归属跟着走，NOTICE 里写清楚了。它还在维护，我们没打算取代它。

做成之后，结果大概是三个端上都用我们，它在安卓上还打不打开，是结果，不是目标。

上游明确要求不接受公开宣传、不引流。这条我们跟着：不宣传、不引流、不做推广。下面红线里的"不商业化"，来路就在这儿。

## 边界

不做的东西不在这里逐条列，全在 docs/decisions.md。这里只留一句原则：

范围不因为上游有、或者别人有就扩大。我们在范围内的每件事上做深，不追功能清单的长度。判断一件事做不做，问的是"它在不在我们的范围内，能不能做得更深"，不是"Han1meViewer 有没有"。

## 拿不准的时候怎么判

**在不在范围内。** 先查 docs/decisions.md 的范围外清单。上游有、竞品有，都不构成做的理由；要点名放行才算数。

**三端怎么算一致。** 浏览和播放的主流程、术语、状态语义，三端必须一致；导航方式、设置怎么组织、桌面大屏的布局，允许按平台来。分不清某处算核心还是外围的时候，把三端同一功能并排看一眼——能一眼认出是同一个 App 的同一个功能，就算核心。

**播放状态要真。** UI 上显示的每个字段，都得能回答"这是引擎的真值，还是我们替它编的"。编的就改掉。同一条在 docs/plan/ 的活计划里是主线，那边是施工，这里是原则。

**四个方向别偏科。** 网络连通、播放与超分辨率、浏览与片库、弹幕，这四块都要做深。任何一块还没到"不妨碍使用"之前，不在另一块上卷到极致。当前重点是网络连接和超分辨率。

**不写没核实的。** 本文档只留判断。涉及外部世界（上游、站点、第三方库）的论断一律去 docs/evidence/，这里只留指针。

**冲突当场问。** 发现本文档和代码、或者和 decisions.md 打架，直接来问我，别自己猜本文档还作不作数。

## 什么叫做成

第一层，三端都能用。每个端各自独立走完这五件事：装机能启动、联网能连通、检索能进条目、播放能播到底且画质可切、弹幕能加载并显示。

第二层，四块都不再碍事。网络连通上换台机器装上就能用，不行也得有明确的降级路径，不能静默失败；播放上 UI 字段就是引擎真值，超分生效、失败降级但绝不断播；浏览片库上收藏、历史、续播都对；弹幕上关联有歧义就不猜，渲染不串集。

第三层是主观的，也是最实在的一条：同一个人做同一件事，用我们更顺。

下层没达成，不许宣称上层。

## 红线

**不碰用户数据。** 不收集、不上传任何使用行为和播放记录。我们没有服务端，这条写死，防止"加个统计吧""埋个点看崩溃"这类提议动摇它。

**不经手内容。** 不提供、不托管、不分发任何媒体内容，流量也不经任何中转服务器。内容来自站点，请求从用户本机出去。

**不商业化。** 没有付费、广告、推广。上游不接受公开宣传，我们也不宣传。

这三条是身份，不是取舍。它们不进 decisions.md 的逐条重认流程——除非改判这个项目是什么，否则不因任何便利而松动。

账号体系和云同步不在红线里。它属于取舍层，当前不在范围内；日后要纳入，按上面的原则走一遍就行，不用改本文档。

## 维护

改这份文档等于改项目的自我定义，得由我逐条确认，agent 不得自行修订。

本文档里凡指向外部世界的结论，都以 docs/evidence/ 里的记录为准；那份记录过期了，这里的说法就跟着失效，需要重测而不是沿用。
