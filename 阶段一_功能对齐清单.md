# 阶段一 · 功能对齐清单 v2

> 更新：2026-09-10 ｜ 用途：**阶段一（1:1 KMP 复刻）** 中 P1「功能对齐」的执行清单
> 基线：`Han1meViewer-main`（Android 原版，326 kt） vs `LoveHan1me`（KMP 版，shared 506 kt）
> v2 变更：**写入用户 2026-09-10 的功能取舍决策**（原 12 项差距的处置已全部敲定）

---

## 0. 决策总表（用户已拍板）

| # | 项目 | 决策 | 动作 |
|---|---|---|---|
| ① | NDK `cpp/`（chino.cpp / kaffu.c） | **砍** | ✅ 已执行（连带解除编译卡死） |
| ② | **视频超分辨率** | **做，是差异点** | 参考 animeko：Desktop(mpv) 最易 → Android(ExoPlayer) → **iOS 不做** |
| ③ | 生物识别 | **全端移除** | ✅ **已执行**（连带应用锁功能整体删除，见 `阶段一_进度报告.md` §3） |
| ④ | 备份 / 恢复 | **全端实现** | `BackupManager` 从 `jvmMain` 上提 commonMain，补 iOS |
| ⑤ | iOS 网络设置页 | **尽量补充** | 走 Darwin engine 配置子集 |
| ⑥ | 下载通知 | **全端移除** | ✅ **已执行（安全子集）**：删 4 类结果通知 + 死代码 UPDATE 渠道；保留前台服务必需的进度通知，见进度报告 §3 |
| ⑦ | `importDownloaded()` Desktop/iOS 恒 false | 保持待办 | Desktop 用 FileKit / iOS 用 DocumentPicker |
| ⑧ | 头像裁剪 `AvatarCropScreen` | **需要对齐** | 从 `app/` 下沉到 commonMain |
| ⑨ | 画中画 | **仅手机端**（Android + iOS），**桌面端不做** | ✅ 桌面端已满足（`shouldEnterPip()` 恒 false，已注释固化）；iOS 仍待补 `AVPictureInPictureController` |
| ⑩ | **Cloudflare 验证** | **重要，需花时间攻坚** | 通路已通（实测修正）；✅ 桌面端自救降级已实现，剩去重实测与 KCEF 失败端到端验证 → 见 §3 |
| ⑪ | 投屏 Cast | **移除** | ✅ **已执行（彻底删，25 文件）**，见进度报告 §3 |
| ⑫ | Glance 小组件 | **全端移除** | ✅ **已执行**（含依赖与三端空壳） |

**净效果**：⑥⑪⑫③ 四项移除**简化了项目**；④⑤⑧⑨⑩ 五项补齐 + ② 超分是主要工作量。

---

## 1. 移除项（删代码，有 git 基线可回退）

| 目标 | 涉及文件 |
|---|---|
| 生物识别 | `app/.../ui/activity/MainActivity.kt` 内 `BiometricPrompt` 逻辑 |
| 下载通知 | `HanimeApplication.kt` 的 NotificationChannel、`HanimeDownloadWorker` 通知回调 |
| 投屏 Cast | `shared/src/androidMain/.../ui/player/CastPlaybackEngine.kt`、`app/.../cast/HanimeCastOptionsProvider.kt`、`PlaybackEngineFactory` 中 Cast 分支、`CastAvailability.*`（`= false` 可留作恒假） |
| Glance 小组件 | `app/.../ui/widget/CheckInWidgetProvider.kt`(227) + `res/xml/checkin_widget_info.xml` + `AndroidManifest` 注册 + `CheckInWidgetUpdate.*`（三端空壳） |
| 桌面端画中画 | `shared/src/desktopMain/.../DesktopVideoPageHost.kt:23` `override fun enterPipMode() {}` |
| NDK（已完成） | `app/src/main/cpp/` + `app/build.gradle.kts` 两处 `externalNativeBuild` |

---

## 2. 补齐项

| 项目 | 动作 | 难度 |
|---|---|---|
| ④ 备份/恢复全端 | `BackupManager.kt`(203 行) 从 `jvmMain` 上提 commonMain，IO 抽 expect（`BackupPickers.{android,desktop,ios}` 已就绪） | 易-中 |
| ⑤ iOS 网络设置页 | 用 `IosCookieBridge.kt` + Darwin engine 配置项子集造阉割版 | 中 |
| ⑦ `importDownloaded()` | Desktop 用 FileKit 目录选择；iOS 用 `UIDocumentPickerViewController` | 中 |
| ⑧ 头像裁剪 | `AvatarCropScreen.kt`(171) 下沉 commonMain（纯 Compose + 平台 ImageDecoder） | 中 |
| ⑨ 手机端画中画 | Android 已有；iOS 接 `AVPictureInPictureController` | 中-难 |

---

## 3. 攻坚项：Cloudflare 验证（⑩，最高优先级）

**为什么重要**：它不是功能，是**用户能否访问应用的前提**。做不好等于 App 打不开。

**现状（2026-09-10 实测修正，原判断有误）**：
- ~~`NetworkPlatform.desktop.kt:17` 显式 `return null`（桌面端无验证）~~ → **不是缺陷**。
  `createCloudflareInterceptor()` 的 expect 声明在 **jvmMain**（`data/network/NetworkPlatform.kt`），
  只覆盖 desktop + android；桌面端靠 **共享总线** 而非拦截器：
  `NetworkRepo.throwRequestException()` 判定 403 + "Just a moment" → `CloudflareChallenges.request(url)`
  → 共享 `App()` 压栈 `CloudflareRoute` → `PlatformScreens.cloudflare` → KCEF 弹窗。
- ~~iOS 无 interceptor 通路~~ → **正确且有意为之**：iOS 不走拦截器，同样走共享总线 →
  `CloudflareVerificationWebView.ios`。iOS 因此不需要 `createCloudflareInterceptor` 的 actual。
- UI 三端齐备（`CloudflareVerificationWebView.ios.kt` / `CloudflareVerificationWindow.desktop.kt`）✓
- 协调器 `CloudflareVerificationCoordinator`(75 行) 留在 `app/`（仅 Android 用，Activity + CountDownLatch），
  **不必强行下沉**——它服务的是 Android 拦截器链路，与共享总线是两条并行通路。

**结论：三端通路已打通，⑩ 剩下的不是「接通路」而是「鲁棒性」。**

**攻坚内容（修订）**：
1. **Desktop KCEF 失败降级（最高优先）**：首次运行需下载约 200MB CEF 运行时，
   国内网络易失败 → 失败后桌面端完全无法过 CF = 打不开应用。
   降级方案：用系统浏览器打开验证页 + 手动粘贴 Cookie
   （`ManualInputCookiesScreen` / `ManualCookiesRouteScreen` 已存在，接上即可）。
2. Android 拦截器与共享总线的去重（"栈上已有 CF 页"逻辑）未测，需实测。
3. iOS 通路完全未经真机验证（Windows 编译不了 iosMain）。
4. 验证失败时的提示与重试体验打磨。
5. **与阶段二 P3 解析代理服务的关系**：代理服务上线后，Cloudflare 可由服务端统一处理。
   但**在此之前客户端必须能自救**，所以本项不能推到 P3。

**参考**：废案 `E:\hanime1\core\network\...\CloudflareChallengeGate.kt`(163) +
`app/desktop/webview2-helper/Program.cs`(489, C# 轮询 `cf_clearance`) 有更完整的实现，可读设计（**不搬代码**）。

---

## 4. 视频超分专项（②，来自 animeko 实现分析）

**animeko 的三端结论**：

| 端 | 实现方式 | 迁移难度 |
|---|---|---|
| **Desktop** | mpv 运行时命令 `change-list glsl-shaders append/remove` + 改 `scale/dscale/cscale=ewa_lanczossharp`、`sigmoid-upscaling`、`deband` 等属性；OFF 时还原原始属性 | **最易（约半天）** |
| **Android** | Media3 `GlEffect` + `BaseGlShaderProgram`（GLES2 API + FP16），`ExoPlayer.setVideoEffects()`；需把 Anime4K 的 mpv 多 pass 拆成单 pass GLSL 模板（`exo-effects/*.frag`） | 中（1–2 天） |
| **iOS** | **animeko 也没做** —— AVPlayer/AVKit 原生管线不支持挂自定义 shader，需 `AVPlayerItemVideoOutput` + Metal 自渲染 | 不做 |

**关键有利条件**：本项目与 animeko **同用 mediamp 0.3.2**，`MPVHandle` 的 `command/option/setPropertyString` API 完全一致 → Desktop 端可照抄。

**本项目现状**：
- 已有 `PlaybackEngine.setSuperResolution(index)` 接口（默认空实现）
- Android `MpvPlaybackEngine` **已用** `change-list glsl-shaders set` ✓
- Android `ExoPlaybackEngine` **未实现** ← 需补 Media3 路径
- 已有 `androidMain/assets/shaders/Anime4K_*.glsl` + `AnimeShaders.copyShaderAssets`（解到 `filesDir/shaders`）
- Desktop 端为 0；iOS 端为 0

**档位设计（抄 animeko）**：`enum { OFF, PERFORMANCE, QUALITY }`，默认 OFF；PERFORMANCE = `Restore_CNN_S`，QUALITY = `Clamp_Highlights + Restore_CNN_VL + Upscale_CNN_x2_VL + AutoDownscalePre + Upscale_CNN_x2_M`。

**⚠️ 必须补的鲁棒性（animeko 的短板，别照抄）**：
- animeko **没有 shader 编译失败回退**，异常直接抛 `VideoFrameProcessingException` → 本项目必须 `try/catch` 失败即降级 OFF
- animeko **没有按设备性能自动降级** → 本项目建议加（低端 GPU 直接禁用 QUALITY）
- Media3 要求 effect graph 在首次 prepare 前预置，否则无法播放中热切换
- Desktop 端需 Gradle 任务把 shader 从 jar 解包到磁盘（mpv 只接受文件路径）；Linux 可直接禁用

**实施顺序**：Desktop mpv（半天）→ Android ExoPlayer（1–2 天）→ 设置/播放器 UI 档位（1–2 天）→ iOS 不做。

---

## 5. 三端能力矩阵（v2，按新决策更新）

| 能力 | Android | Desktop | iOS |
|---|:--:|:--:|:--:|
| 网络请求 / HTML 解析 / 图片加载 | ✅ | ✅ | ✅ |
| 视频播放 | ✅ Exo/Mpv/System | ✅ DesktopMpv | ✅ IosAV |
| 下载 | ✅ | ⚠️ 无通知（决定移除） | ⚠️ 同左 |
| 数据库（Room KMP）/ DataStore | ✅ | ✅ | ✅ |
| 文件选择 | ✅ | ⚠️ 仅备份选择器 | ⚠️ 同左 |
| 备份 / 恢复 | ✅ | ✅ | **🔨 待补（④）** |
| 网络设置页 | ✅ | ✅ | **🔨 待补（⑤）** |
| 头像裁剪 | ✅ | **🔨 待下沉（⑧）** | **🔨 待下沉（⑧）** |
| 画中画 | ✅ | ❌ **不做（⑨）** | **🔨 待补（⑨）** |
| Cloudflare 验证 | ✅ | **🔨 攻坚（⑩）** | **🔨 攻坚（⑩）** |
| 视频超分 | 🔨 补 Exo 路径（②） | **🔨 最易（②）** | ❌ 不做（②） |
| 崩溃恢复 / 更新检查 | ✅ | ✅ | ✅ |
| ~~生物识别~~ | ❌ 移除 | ❌ 移除 | ❌ 移除 |
| ~~下载通知~~ | ❌ 移除 | ❌ 移除 | ❌ 移除 |
| ~~投屏~~ | ❌ 移除 | N/A | ❌ 移除 |
| ~~小组件~~ | ❌ 移除 | N/A | ❌ 移除 |
| 伪装图标 | ✅ 保留（待定） | N/A | N/A |

---

## 6. P1 出口标准

- [ ] 移除项全部删除（生物识别 / 下载通知 / Cast / Glance / 桌面 PiP / NDK）
- [ ] ④⑤⑦⑧ 补齐（备份全端、iOS 网络设置、importDownloaded、头像裁剪）
- [ ] ⑨ 手机端画中画补齐（Android 已有 + iOS）
- [ ] ⑩ **Cloudflare 三端可用**（攻坚项，不容降级）
- [ ] ② 超分：Desktop + Android 可用，iOS 明确标注不支持
- [ ] `app/` 残留按三类处置完毕（砍 / 下沉 / 保留 actual）
- [ ] 三端均可编译运行，功能与原 Android 版对齐，无 P0 崩溃
