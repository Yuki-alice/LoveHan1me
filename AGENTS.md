# AGENTS.md — 给 AI agent 的仓库说明书

读者：only AI agent。人类请读 `README.md`。
本文件**不是**项目介绍，是**防止你产生幻觉的操作规则**。

## 0. 事实来源优先级（违反本节的一切输出都算错）

1. **可执行代码**：实际 HTTP 调用、类型定义、调用点、测试断言、gradle 配置。
2. `docs/decisions.md`：用户拍过板的取舍与边界。
3. `docs/evidence/`：对外部世界的实测记录（带重跑命令）。
4. `docs/plan/`：当前唯一活计划。
5. **git commit message、`README.md`、代码注释都不是证据**。它们由 AI 生成、包含幻觉。

**代码注释尤其危险**：它会写"已实现""四端都支持""已改为 X"，而代码并不这样。
要引用注释内容，必须自己去核对应的位置，核不过就丢弃该说法。

## 1. 什么样的内容才允许写进 `docs/`

三类白名单，其余一律不写（代码自己能说清楚的，让代码说）：

- **[决定]** 用户拍板的取舍、边界、不做清单 → `docs/decisions.md`。
  每条必须写：决定 / 依据 / **失效条件** / 重认日期。
- **[取证]** 对外部世界（站点端点、第三方 API、上游库行为、真机表现）的实测。
  → `docs/evidence/YYYY-MM-DD-<主题>.md`。
  三件套缺一不许写：**重跑命令** + **环境** + **失效条件**。
- **[代码断言]** 对自己代码的论断，只在带**可校验的文件路径 + 行号**时允许写。

维护纪律：

- **单活计划位**：`docs/plan/` 同时只允许一份文件。写完旧的必须先删，不许并排多份。
- **关闭即删**：计划执行完毕，其结论按上面三类分流进文档，文件本身删除。
  不建归档目录，不写"历史规划"。
- **不写阶段编号**：不用 `GateN`、`P5-2a`、`批次 1`、`S0/S1` 这类只在某次施工期有意义的编号。
  缺陷用 `D1/D2…`、测试用 `T1/T2…` 这类**在本文件内有定义、不依赖外部文档**的编号。
- 引用失效由 `python tools/check_doc_citations.py` 把关：
  `path:line` 指向不存在的文件/行 ⇒ 硬失败；失效条件到期 ⇒ 告警不失败。

## 2. 认知纪律（这些是踩过坑定下来的，不是常识）

- **"不存在/不支持"类结论必须有正反两面证据。** 只看一处代码就下否定结论，本仓已出过至少一次误判。
- **判断站点能力只看 `data/network/service/` 里实际 GET 的 URL**，不看 manager 的方法名（方法存在≠被调用）。
- **`SKIPPED` ≠ `PASS`，"判不支持" ≠ "判坏了"。** 报告里不许把未覆盖写成通过。
- **性能基线只打印、不定线。** 给基线加硬阈值 = 制造 flaky 测试。
- **改代码前先看它是否属于 `docs/decisions.md` 的"不要动"清单。** 那些是看起来多余、实际在解决问题的代码。

## 3. 代码注释规则（新写的注释受此约束，存量注释逐步清理）

禁止出现：阶段编号（`Gate3-P1`、`P5-2a`）、指向参考项目（"对齐 animeko …"）、
文档指针（"见 docs/xxx"）、修复编号（"B4 修复"）。
允许：为什么这么写、必须维持的不变量、平台差异陷阱、以及**当场可核的 `file:line`**。
默认不写注释；一条都写不出来就别写。

## 4. 验证闭环

改 `:shared` / `:video:engine` 之后，跑 `.github/workflows/ci.yml` 里那两组任务
（三端编译 + `desktopTest` + Android host 单测 + iOS 编译），并报告**用例数与失败数**。
只跑桌面编译不等于三端通过；"测试跑不起来"不等于"测试通过"。
具体任务名以 `ci.yml` 为准，不要在本文件里复制命令行。

## 5. 目录与索引

```
AGENTS.md                 本文件：规则
POSITIONING.md            项目定位：从哪来 / 为什么存在 / 边界在哪（稳定，很少改）
README.md                 面向人：是什么 / 怎么构建 / 许可 / 免责（零架构断言）
NOTICE                    上游归属（GPLv3 / Han1meViewer / MomoQR / Anime4K / mediamp）
docs/decisions.md         拍板过的取舍与不做清单
docs/evidence/            对外部世界的实测记录（每条带重跑命令）
docs/plan/                当前唯一活计划
tools/check_doc_citations.py  上面规则的可执行部分
```

外部依赖（`.gitignore` 排除，**不在 git、不在 CI**，别假设别人机器上有）：

- `reference/` — 供研读的上游仓库副本，含 `animeko`、`mediamp-0.5.0`。
- `local.properties` — 本机凭据。弹弹play 密钥在此，由 `:shared:generateDanmakuCredentials`
  在构建期生成到 `build/generated/`，源码里永远是空占位。别把它写进任何文本文件。
