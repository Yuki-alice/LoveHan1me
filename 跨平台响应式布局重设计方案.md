# LoveHan1me 跨平台响应式布局重设计方案

> 输入：项目布局代码静态盘点 + Material Design 3 官方布局规范（m3.material.io，含 I/O 2026 更新）+ 同类跨平台流媒体应用调研
> 日期：2026-09-11
> 配套文档：《前端设计体系审计报告.md》（token 层面问题）
> 本文档聚焦**布局与导航形态**，不含颜色/字阶/形状的 token 收敛

---

## 0. 一句话结论

项目目前的布局本质是**「一套手机布局 + 一个宽度阈值」**：所有断点只在两个地方起作用
（是否上常驻抽屉、网格几列），窗格数、导航形态、内容限宽都不随宽度变化。

对照 MD3 五档断点规范，缺的不是"更多的断点"，而是**三个结构性的东西**：

1. **没有导航形态的中间态** —— 要么模态抽屉（手机），要么 360dp 常驻抽屉（≥840dp），没有 NavigationRail。
2. **没有第二个窗格** —— 全项目零 `ListDetailPaneScaffold` / `SupportingPaneScaffold`，
   唯一的双栏（视频页相关推荐）还被用户开关 + 物理横屏双重门控。
3. **没有栅格体系** —— 有列数，但没有 margin / gutter 规则，也没有内容最大行宽（840dp 限宽只用在设置页）。

---

## 1. 现状盘点（代码事实）

| # | 布局决策 | 现状 | 位置 |
|---|---|---|---|
| 1 | 断点档位 | 4 档：350 / 600 / 840 / 1200 | `ui/adaptive/Breakpoints.kt:28-45` |
| 2 | 导航形态 | 只有两种：ModalNavigationDrawer / PermanentNavigationDrawer | `app/App.kt:226-273` |
| 3 | 底部导航栏 | **不存在**（`NavigationBar` 全项目 0 次） | — |
| 4 | 导航栏 Rail | **不存在**（`NavigationRail` 全项目 0 次） | — |
| 5 | 常驻抽屉阈值 | 整窗宽 ≥ 840dp（`windowWidthSizeClassOf` ≥ Expanded） | `app/App.kt:155` |
| 6 | 常驻抽屉占宽 | 约 360dp（`PermanentDrawerSheet` 默认） | `app/App.kt:230-238` |
| 7 | 内容区宽度采样 | ✅ 正确扣除 chrome 占宽后下发 | `ui/adaptive/WindowSize.kt:60-67` |
| 8 | 内容最大行宽 | 840dp，但**仅用于设置页** | `app/navigation/settings/SettingsNavHost.kt:60` |
| 9 | 网格列数（竖版） | 2 / 3 / 4 / 5 @ 600 / 840 / 1200 | `ui/model/SearchGridColumnsConfig.kt` |
| 10 | 横向卡片可见数 | 1.5 / 2.1 / 4.1 / 5.1 @ 350 / 600 / 840 | `ui/model/HorizontalCardCountConfig.kt` |
| 11 | 首页结构 | 单列 `LazyColumn`：banner → 更新卡 → 公告 → N × 横向分类行 | `feature/home/homepage/HomePageContent.kt:56-131` |
| 12 | 分类行 | `LazyRow` 横向滚动，卡片宽 = 内容宽 / 可见数 | `.../component/CategoryRow.kt:78-94` |
| 13 | 视频详情双栏 | 存在，但需 `tabletMode` **且** 物理横屏 | `feature/video/VideoShellContent.kt:127-128` |
| 14 | 双栏宽度 | `maxWidth * 0.38f`（随窗口比例变化） | `VideoShellContent.kt:329,379` |
| 15 | 播放器高度 | 固定 250dp（普通）/ 400 或 500dp（平板模式） | `feature/video/VideoRouteHostScreen.kt:512-516` |
| 16 | 大屏判定 | 内容宽 ≥ 600dp，但仅用于弹一句"建议开平板模式"的 toast | `VideoRouteHostScreen.kt:147-169` |
| 17 | 双栏/多窗格 API | `ListDetailPaneScaffold` / `SupportingPaneScaffold` 全项目 0 次 | — |
| 18 | 自适应门控 | 视频页布局被 `appSettings.tabletMode` 决定 | `VideoShellContent.kt:127` |
| 19 | 设置页密度项可见性 | `tabletMode \|\| 宽度 ≥ Expanded` | `feature/settings/HomeSettingsScreen.kt:239` |

**最关键的三个反模式：**

- **⑬⑱ 布局被用户开关决定，而不是被可用空间决定。** 用户把桌面窗口拉到 2560dp，
  视频页依然是 250dp 播放器 + 单列评论，除非他手动去设置里打开"平板模式"。
  代码里甚至为此写了一个 toast 去提醒用户打开开关（`VideoRouteHostScreen.kt:157-169`）——
  这是把响应式的责任推给了用户。
- **⑫ 宽屏下的横向滚动。** `CategoryRow` 在 1920dp 桌面上只显示 5.1 张卡片，其余靠横向滚动。
  触屏上滑动是自然的，**鼠标滚轮是纵向的** —— 桌面用户要按住 Shift+滚轮才能横向滚动。
  实测 YouTube 桌面版所有内容区都是纵向滚动 + 网格，没有横向滚动分类行。
- **⑨⑩ 有列数但没有栅格。** 列数是"能塞几张卡"，没有 margin / gutter 规则，
  也没有最大行宽。1920dp 下 5 列卡片会拉得很大，而文字行会拉到 1800dp 宽（远超可读行宽）。

---

## 2. 对照 MD3 官方规范

### 2.1 断点：MD3 是 5 档，项目是 4 档

来源：m3.material.io/foundations/layout/breakpoints。注意 MD3 已把
"window size class" 正式更名为 **breakpoint**。

| Breakpoint | 宽度 (dp) | 典型设备 | 项目现状 |
|---|---|---|---|
| Compact | < 600 | 手机竖屏 | ✅ Compact |
| Medium | 600–839 | 平板竖屏、折叠屏展开竖屏 | ✅ Medium |
| Expanded | 840–1199 | 手机横屏、平板横屏、折叠屏展开横屏、桌面 | ✅ Expanded |
| Large | 1200–1599 | 桌面 | ✅ Large |
| **Extra-large** | **1600+** | 桌面、超宽显示器 | ❌ **缺失** |

另外 MD3 的 `Large` 下界是 **1200**，项目的 `Large = 1200` 对齐正确。
项目额外的 `Narrow = 350`（超窄亚档）是合理扩展，但应明确它是**内容密度亚档而非导航断点**。

### 2.2 窗格数：MD3 给了明确的档位映射

> "Compact and medium breakpoints: a single pane works best / Expanded and large: two panes
> are recommended / Extra-large: consider using three panes"

| 断点 | 推荐窗格 | 项目现状 |
|---|---|---|
| Compact | 1 | 1 ✅ |
| Medium | 1（推荐）；低密度内容可用 2 | 1 |
| Expanded | 2（推荐） | 1 ❌ |
| Large | 2（推荐） | 1 ❌ |
| Extra-large | **3** | 1 ❌ |

### 2.3 导航形态：MD3 的推荐路径与项目完全不同

规范原文两条关键结论：

> "a bottom navigation bar in a compact layout can be swapped with a navigation rail in a medium layout"
> "a collapsed navigation rail in medium or expanded layouts can become an expanded
> navigation rail in large or extra-large layouts"

| 断点 | MD3 推荐 | 项目现状 | 差距 |
|---|---|---|---|
| Compact | Navigation bar | Modal drawer（汉堡） | ⚠️ 一级入口不可见 |
| Medium | Collapsed navigation rail | **Modal drawer** | ❌ 未利用 rail |
| Expanded | Collapsed navigation rail | **Permanent drawer（360dp）** | ❌ 形态不符 |
| Large | Expanded navigation rail | **Permanent drawer（360dp）** | ❌ 形态不符 |
| Extra-large | Expanded navigation rail | **Permanent drawer（360dp）** | ❌ 形态不符 |

**成本对比**：常驻抽屉在 840dp 窗口下吃掉 **43%** 的宽度；折叠 rail 只吃约 **80dp（9.5%）**，
展开 rail 约 220–240dp。也就是说，**从 840dp 到 1199dp 这一段，项目每天都在浪费约 280dp 的内容宽度。**

### 2.4 栅格：MD3 的经典定义

| 屏幕宽度 | 栅格列 | Margin | Gutter |
|---|---|---|---|
| < 600dp | 4 | 16dp | 8dp |
| 600–904dp | 8 | 32dp | 24dp |
| ≥ 905dp | 12 | 32dp | 24dp |

项目现状：有列数（2/3/4/5），但没有 margin / gutter 体系，页面普遍写 12dp 横向内边距
（`HomePageContent.kt:69`、`CategoryRow.kt:57`），宽屏下显得贴边。

### 2.5 行宽：MD3 明确给了可读性红线

> "Across all breakpoints, adjust margins and type styles to keep text between
> **40–60 characters per line**."

按 16sp 正文（拉丁文均宽约 0.53em ≈ 8.5dp）估算：40 字符 ≈ 340dp，60 字符 ≈ 510dp。
项目设置页的 `contentMax = 840dp` 大约对应 **98 字符/行**，**明显超出**规范红线。
而其他页面（首页、搜索、视频详情、评论）**完全没有限宽**，桌面全屏下行宽可以到 1800dp+。

---

## 3. 同类优秀应用怎么做的

| 应用 | 大屏做法 | 可借鉴点 |
|---|---|---|
| **YouTube**（平板横屏双栏） | 左侧 navigation rail 承载频道更新，右侧内容列；缩略图放大；评价是"更少的空白、更少的旋转" | rail 不只是导航，还能承载**局部内容分区**；从竖屏到横屏不是"旋转"，是重排 |
| **YouTube**（超宽三栏） | `comments | player | related`，related 栏 `min-width:300px / max-width:420px`，primary `1260–1640px`，三栏断点在 1599px | **相关推荐是固定像素宽，不是百分比** —— 这是本方案要做的最直接改动 |
| **YouTube**（旧版教训） | 曾长期"只是把竖屏布局旋转一下"，被媒体批评为 *"the lazy way to build a landscape experience"* | 项目当前状态与这段批评高度重合 |
| **Netflix** | landscape-first、更大缩略图、更清晰的分类分组、播放控制极简 | 大屏不是"更多信息"，而是"更少干扰 + 更大图像" |
| **Plex / Jellyfin** | 海报墙（Poster Wall）、library 视图与播放器分离 | 媒体库形态：桌面用高密度海报墙，移动用低密度大卡 |
| **Twitch** | 直播 + 聊天分栏，聊天栏固定宽度常驻 | 与视频页"播放器 + 评论/简介"同构 |
| **Spotify** | 平板横屏：更宽封面、更清晰的队列管理 | 底部 bar → 侧边栏的过渡 |

**提炼出三条原则：**

1. **宽屏要多的是"窗格"，不是"更大的卡片"。** MD3 原话："Additional space doesn't just mean
   making the same thing bigger." 项目当前的列数增长方向是对的，但缺少"第二个窗格"。
2. **侧栏宽度要固定，不要百分比。** 相关推荐/评论这类附属内容，宽度应固定在 320–420dp 区间；
   用 `38%` 会在 2560dp 窗口下变成 970dp 的巨型侧栏。
3. **导航 chrome 的宽度是设计决策，不是默认值。** 360dp 常驻抽屉在 840dp 窗口下等于自杀。

---

## 4. 重设计方案

### 4.1 断点体系：补第五档

```kotlin
// ui/adaptive/Breakpoints.kt
object WindowWidthBreakpoints {
    /** 内容密度亚档（非导航断点）。 */
    const val Narrow = 350

    const val Compact = 0
    const val Medium = 600
    const val Expanded = 840
    const val Large = 1200
    const val ExtraLarge = 1600     // ← 新增
}

enum class WindowWidthSizeClass { Compact, Medium, Expanded, Large, ExtraLarge }
```

> `Narrow = 350` 保留但语义收窄：**只有卡片密度/横向可见数可以用它**，
> 导航与窗格决策一律走 600/840/1200/1600。

### 4.2 导航形态矩阵 ★本方案的核心

| 断点 | 宽度 | 一级导航 | 二级入口 | 窗格数 |
|---|---|---|---|---|
| Compact | < 600 | **NavigationBar**（4 项：首页 / 搜索 / 收藏 / 我的） | 汉堡 + Modal drawer（全量入口，含设置、下载、历史、订阅…） | 1 |
| Medium | 600–839 | **Collapsed NavigationRail**（约 80dp，图标 + 选中指示器） | 顶栏 actions | 1 |
| Expanded | 840–1199 | **Collapsed NavigationRail**（约 80dp） | 顶栏 actions | **2** |
| Large | 1200–1599 | **Expanded NavigationRail**（约 220dp，图标 + 文字标签） | 内联 | **2** |
| Extra-large | 1600+ | **Expanded NavigationRail** | 内联 | **3** |

**为什么 NavigationRail 一定能赢过 PermanentDrawer：**

| | PermanentNavigationDrawer | Collapsed NavigationRail | Expanded NavigationRail |
|---|---|---|---|
| 占宽 | ~360dp | ~80dp | ~220dp |
| 840dp 窗口下占比 | 43% | 9.5% | 26% |
| 1200dp 窗口下占比 | 30% | 6.7% | 18% |
| 入口可见性 | 全部可见 | 图标可见（tooltip 补全） | 全部可见 |

关键改动点：

1. **`app/App.kt` 的 `usePermanentDrawer` 分支整体废弃**，改为 `NavigationSuiteScaffold`
   或手写三态切换（CMP 是否有 `NavigationSuiteScaffold` 需先验证，见 §7 风险 1）。
2. 抽屉降级为 **Compact 专属的"全量入口"**，不再承担 Medium+ 的一级导航。
   现有 `MainDrawerContent` 内容可以整体复用，只是从"常驻"改为"模态"。
3. **`ProvideContentWidth` 的扣除值必须同步更新** —— 现在是硬编码假设 360dp，
   改 rail 后要跟随实际 chrome 宽度（80 / 220 / 0）。

### 4.3 栅格与间距

**间距阶梯**（M3 8dp 基准，同时也解决审计报告里的双源冲突）：

```
4 / 8 / 12 / 16 / 24 / 32 / 48
```

**各断点的 margin / gutter / 列数：**

| 断点 | Margin | Gutter | 竖版卡列数 | 横版卡可见数 | 内容最大行宽 |
|---|---|---|---|---|---|
| Compact (<600) | 16 | 8 | 2 | 1.5 | 不限（窄屏天然安全） |
| Medium (600–839) | 24 | 16 | 3 | 3 | 不限 |
| Expanded (840–1199) | 32 | 24 | 4 | 4 | 720 |
| Large (1200–1599) | 32 | 24 | 5 | 6 | 720 |
| Extra-large (1600+) | 32 | 24 | 6 | 8 | 720 |

**内容最大行宽（新增的两条规则）：**

| 内容类型 | 最大宽度 | 理由 |
|---|---|---|
| 表单 / 设置行 | **640dp** | 当前 840dp 约 98 字符/行，超出 MD3 的 40–60 字符红线；收到 640dp ≈ 75 字符，配合 `title + summary` 两段式行结构是合理上限 |
| 正文 / 简介 / 评论 | **720dp** | 纯阅读内容，略宽于表单，居中排布 |
| 卡片网格区 | 不限宽 | 靠列数控制密度，`margin/gutter` 保证不贴边 |

> 实现建议：在 `ui/adaptive/` 增加一个 `ContentColumn` 容器（`widthIn(max = …)` + 居中），
> 而不是每个页面各写一遍 `Box + widthIn + fillMaxSize`（当前只有设置页这么做了）。

### 4.4 各页面布局规格

#### （1）首页 Feed

**现状问题**：宽屏下仍是单列 + 横向滚动分类行（§1 反模式 ⑫）。

| 断点 | 布局 |
|---|---|
| Compact | 保持现状：banner → 卡片 → 公告 → 横向分类行（`LazyRow` 是触屏正确形态） |
| Medium | banner 限宽 720dp 居中；分类行横向可见数提到 3 |
| **Expanded+** | **分类行改为网格块**：每个分类渲染为 `LazyVerticalGrid`，默认 2 行（超出用"更多"进入分类页）。彻底去掉宽屏横向滚动 |
| Large+ | 顶部进入 **Feed canonical layout**：banner 缩为左侧 hero（约 60% 宽），右侧放"继续观看"/"追番更新"纵向小列表 |

#### （2）搜索 / 收藏 / 历史 / 下载（网格页）

| 断点 | 布局 |
|---|---|
| Compact / Medium | 现状单列网格（列数按 §4.3 表） |
| Expanded+ | 左侧**常驻筛选栏**（约 240dp，放排序/分类/年份等当前在弹层里的筛选器）+ 右侧网格 |
| Extra-large | 筛选栏 + 网格 + （可选）详情预览栏 |

#### （3）视频详情页 ★改动最大

**现状**：单列滚动；必须"开关 ON + 物理横屏"才有右侧相关推荐，且侧栏是 `38%`。

| 断点 | 布局 |
|---|---|
| Compact | 单列：播放器 → 标题/元信息 → 操作行 → tabs（简介/评论） |
| **Medium+** | **左右双栏**：主栏（播放器 + 标题 + 操作 + tabs）+ 右栏**固定 360dp** 相关推荐 |
| Expanded+ | 在双栏基础上，左导航 rail 常驻 → 视觉上成三区（rail / 主栏 / 相关） |
| Extra-large | 主栏内部再分：播放器 + 简介左右并排（评论区独立在下方全宽） |

**两个具体规格改动：**

1. **侧栏宽度从 `maxWidth * 0.38f` 改为固定 `360dp`**
   （对齐 YouTube 的 `min-width:300 / max-width:420` 区间）。
   在 2560dp 窗口下，`38%` 会得到 973dp 的巨型侧栏，注意力被完全拉走。
2. **播放器高度从固定 `250.dp` 改为比例式**：
   ```kotlin
   // 主栏宽 × 9/16 与主栏高 × 0.55 取小，兜底 240dp
   val playerHeight = min(mainPaneWidth * 9f / 16f, mainPaneHeight * 0.55f)
   ```
   固定 250dp 在 1920dp 宽的桌面窗口下只剩 13% 宽度占比，播放器小得荒谬。
3. **去掉 `tabletMode` 门控**：双栏由断点决定。`tabletMode` 降级为
   "用户强制覆盖"（可强制开/强制关），默认跟随宽度。

#### （4）播放 / 全屏

| 断点 | 布局 |
|---|---|
| Compact | 现状：播放器在上，tabs 在下 |
| Medium+ | 播放器铺满可用区域（保持 16:9，`Box` 居中 letterbox），tabs 移入**右侧固定 400dp** 栏 |
| 桌面全屏 | 播放器 + 右侧栏 + 底部常驻控制条（不再自动隐藏，因为鼠标不需要"沉浸"） |

#### （5）设置页

**现状**：单列 + 840dp 限宽。

| 断点 | 布局 |
|---|---|
| Compact | 现状钻取式：列表 → 点进二级页 |
| **Medium+** | **List-Detail**：左侧分类列表（约 280dp）+ 右侧当前分类详情，取消二级页跳转 |
| 限宽 | 从 840dp 收到 **640dp**（正文列） |

> 设置页是 MD3 明确点名的 List-Detail 适用场景：
> "List-detail can also be used for less critical paths such as dividing app preferences
> into a list of categories with the preferences for each category in the detail pane."

---

## 5. 现状 vs 提案 对照表

| 维度 | 现状 | 提案 |
|---|---|---|
| 断点 | 4 档（350/600/840/1200） | 5 档（+1600），Narrow 降为密度亚档 |
| 导航（<600） | 汉堡 + Modal drawer | **NavigationBar + Modal drawer（全量）** |
| 导航（600–839） | Modal drawer | **Collapsed NavigationRail** |
| 导航（840–1199） | Permanent drawer（360dp） | **Collapsed NavigationRail（80dp）** |
| 导航（1200–1599） | Permanent drawer | **Expanded NavigationRail（220dp）** |
| 导航（1600+） | Permanent drawer | **Expanded NavigationRail** |
| 窗格数 | 全档 1（视频页例外且需开关） | 1 / 1 / 2 / 2 / 3 |
| 栅格 | 只有列数 | margin + gutter + 列数 完整体系 |
| 内容限宽 | 仅设置页 840dp | 表单 640dp / 正文 720dp，全局生效 |
| 视频页相关推荐 | 开关 + 物理横屏，38% 宽 | 断点驱动，固定 360dp |
| 播放器高度 | 固定 250dp | 比例式，随主栏宽高 |
| 宽屏横向滚动 | 首页分类行 | Expanded+ 改网格块 |
| 自适应门控 | `tabletMode` 决定布局 | 宽度决定，开关降级为覆盖 |

---

## 6. 迁移路径

建议按"每一步都能独立编译验证 + 独立回滚"来切，每步结束跑
`./gradlew :shared:compileKotlinDesktop :shared:compileAndroidMain :shared:compileKotlinIosSimulatorArm64`。

| 阶段 | 内容 | 预估 | 验收 |
|---|---|---|---|
| **P0** | 间距单源收敛、断点补 `ExtraLarge`、`Narrow` 语义收窄 | 0.5d | 三端编译通过；无 `SpacingMedium` 歧义引用 |
| **P1** | 导航形态矩阵：NavigationBar(compact) + NavigationRail(medium+)；`ProvideContentWidth` 扣除值跟随 chrome 实际宽度 | 2–3d | 各断点下导航形态正确；内容区宽度采样无超算 |
| **P2** | 栅格与限宽：margin/gutter 表、`ContentColumn` 容器、列数对齐新表 | 1–2d | 1920dp 下无贴边、正文行宽 ≤ 720dp |
| **P3** | 视频详情双栏（断点驱动 + 固定 360dp 侧栏 + 比例播放器高度） | 2–3d | 去掉 `tabletMode` 依赖；1920dp 下播放器占比合理 |
| **P4** | 设置页 + 搜索页 List-Detail | 2–3d | Medium+ 下第二窗格出现；返回行为正确 |
| **P5** | 首页宽屏网格块 + XL 三窗格 | 1–2d | 宽屏无横向滚动 |

**总计约 9–14 个工作日。**

---

## 7. 风险与取舍（需要你判断的）

1. **`material3-adaptive` 依赖不在当前缓存里。**
   本机 Gradle 缓存只有 `org.jetbrains.compose.material3:material3*`，**没有 `material3-adaptive`**。
   CMP 1.12.0 理论上提供移植版（`ListDetailPaneScaffold` / `SupportingPaneScaffold`），
   但需要首次联网解析，而本机走代理、对 github/dl.google.com 有间歇 502。
   **建议**：先在独立分支试加依赖验证可解析；若不可用，自研一个简化 PaneScaffold
   （只需处理 `directive = 断点 → 窗格可见性`，估 1–2 天），项目已有 `ui/adaptive/` 基础，成本可控。

2. **手机上引入底部导航栏是 breaking change。**
   现在手机用户靠汉堡 + 抽屉导航，改成底部 bar 会改变肌肉记忆。
   但 MD3 规范明确 compact 用 navigation bar，且主流应用（YouTube/Bilibili/Netflix）手机端全是底部栏。
   需你确认是否接受这个改动；也可以先只在 Medium+ 上 rail，compact 先保持抽屉观察。

3. **三端平台惯例差异。**
   iOS 的 TabBar（底部）与 Android 的 NavigationBar 形态接近，可以统一；
   iPad 惯例是 sidebar（≈ 展开 rail），也与本方案一致。
   但 iOS 的**边缘右滑返回**与 rail 布局叠加需要实机验证（项目已有 iOS 模拟器环境）。

4. **`tabletMode` 开关的存废。**
   现状是三处依赖（视频页布局、设置页密度项可见性、播放器高度）。
   建议保留为"强制覆盖"语义，默认改为宽度驱动 —— 但这意味着**同一台设备上默认行为会变**。

5. **常驻抽屉改 rail 后的信息架构问题。**
   抽屉现在承载了 4 组共 11 个入口（首页/设置/签到 + 我的列表 4 项 + 视频 2 项）。
   NavigationRail 只适合 3–7 个一级入口。需要重新做一次**一级/二级入口的划分**，
   这是本方案里唯一需要动的信息架构，不只是布局。

---

## 8. 参照链接

- MD3 Breakpoints：https://m3.material.io/foundations/layout/breakpoints/overview
- MD3 Windows size classes（含导航/窗格推荐表）：https://m3.material.io/foundations/layout/applying-layout/window-size-classes
- MD3 Canonical layouts（feed / list-detail / supporting pane）：https://m3.material.io/foundations/layout/canonical-layouts
- MD3 I/O 2026 更新（Expressive layout、新 8dp 间距体系、scaffold）：https://m3.material.io/blog/whats-new-at-io26
- Android Canonical layouts（Compose 实现）：https://developer.android.com/develop/ui/compose/layouts/adaptive/canonical-layouts
