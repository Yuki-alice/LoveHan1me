# 跨平台布局原型 —— Agent 可读格式

> 来源：Ardot 画布 fileId `724724560492703`
> 转换日期：2026-09-12
> 转换方：WorkBuddy 设计助手（design-to-code 流程）
> 用途：让任何下游 agent / LLM 在没有 Ardot 客户端的情况下理解这套布局

---

## 0. 一句话定位

这是 LoveHan1me **跨平台布局重构**阶段的视觉原型，与下列文档互为参照：

- 《导航重构与设计原型方案.md》—— 信息架构与功能层面的设计
- 《跨平台响应式布局重设计方案.md》—— 断点与布局规则的工程依据
- 《前端设计体系审计报告.md》—— token 层（颜色 / 字阶 / 形状）合规性

**重要**：本原型里**看到的**形态与代码**最终实现的**形态并不逐像素一致——多个 P 已落地为有意偏离（例如分类矩阵改用 `GridCells.Adaptive` + 最小卡宽分档，而非文档里 2/3/4/5/6 的固定列数表）。以代码为准、以原型为意图对照。

---

## 1. 原型画布元数据

| 项 | 值 |
|---|---|
| 平台 | Ardot（Compose-style 画布） |
| fileId | `724724560492703` |
| fileUrl | `https://ardot.tencent.com/file/724724560492703` |
| 页面数 | 1 |
| 顶层节点 | 16（其中 11 个屏幕 + 3 个章节标题区 + 2 个规格说明块） |
| 屏幕尺寸覆盖 | 393 / 768 / 1024 / 1440 / 1600（dp 与 px 一一对应） |

---

## 2. 设计系统（贯穿所有屏幕）

### 2.1 颜色（取自原型内常见填充值）

| 用途 | 颜色 | 备注 |
|---|---|---|
| 页面背景 | `#fdf8fa` | 偏暖的近白色，接近 M3 surfaceContainer |
| 卡片背景（中性） | `#f6edef` | 接近 surfaceBright |
| 卡片背景（弱化） | `#f1e1e5` | 接近 surfaceContainerLow |
| 强调背景（次级） | `#f5dde5` | 接近 secondaryContainer |
| 强调背景（次级弱化） | `#ecdedf` | 接近 surfaceContainerHighest |
| 强调背景（三级） | `#ffd9e2` | tertiaryContainer |
| 主文本 | `#1f1b1b` | 接近 onSurface |
| 次文本 | `#524344` | 中等对比度 |
| 辅助文本 | `#8a7278` | 低对比度 |
| 强调色 | `#9f4058` | 用于「取消」「恢复默认」等动作色 |

### 2.2 形状（圆角阶梯）

```
12 / 16 / 20 / 24 / 28 —— 卡片 / 分区 / 行高
50% / 99 dp —— pill / avatar
```

### 2.3 字阶

| 角色 | 字体族 | 字重 | 字号范围 | 出现位置 |
|---|---|---|---|---|
| 页面主标题 | Noto Sans SC | Bold | 26–30 | 各章节标题区 |
| 一级标题 | Noto Sans SC | SemiBold | 18–22 | 页面标题、Banner 文字 |
| 二级 / 操作 | Noto Sans SC | SemiBold | 14–18 | 卡片标题、tab |
| 正文 | Noto Sans SC | Regular | 12–15 | 简介 / 元信息 |
| 状态数字 | Inter | SemiBold | 13–15 | 状态栏时间、拉丁字符 |

### 2.4 间距阶梯

```
4 / 8 / 12 / 16 / 20 / 24 / 28 / 44（gap 与 padding 同尺度）
```

---

## 3. 断点 × 形态矩阵

| 断点 | 宽度 | 导航形态 | 一级窗格 | 出现于本原型的屏幕 |
|---|---|---|---|---|
| Compact | < 600dp | **NavigationBar**（底部 3 项：首页 / 发现 / 我的） | 1 窗格 | 3:4, 3:5, 3:811, 3:889, 3:1118 |
| Medium | 600–839dp | **Collapsed NavigationRail**（80dp） | 1 窗格 | 3:527（首页 3×3） |
| Expanded | 840–1199dp | **Collapsed NavigationRail**（80dp） | 1 窗格 | 3:6（首页）, 3:657（我的宫格） |
| Large | 1200–1599dp | **Expanded NavigationRail**（220dp） | 2 窗格 | 3:7（视频详情 + 侧栏）, 3:1036（设置双栏） |
| ExtraLarge | 1600+ | **Expanded NavigationRail**（220dp） | 2 窗格 | 3:592（首页 5×2） |

> 关键决策：**无 NavigationRail** 的两个例外——
> - 设置页（3:1036, 3:1118）：任何断点都不挂 Rail，**独立全屏路由**
> - 视频详情（3:889 Compact）：Compact 下也没 Rail，但其它断点有

---

## 4. 屏幕索引（按行排列）

| nodeId | 名称 | 尺寸 | 断点 | 行号 |
|---|---|---|---|---|
| 3:1 | 标题区 | 3560×92 | — | R0（章节标题） |
| 3:4 | 手机·首页（Compact） | 393×1080 | Compact | R1 |
| 3:5 | 手机·我的（Compact） | 393×852 | Compact | R1 |
| 3:6 | 平板横屏·首页（Expanded） | 1024×768 | Expanded | R1 |
| 3:7 | 桌面宽屏·视频详情（Large） | 1440×1000 | Large | R1 |
| 3:8 | 导航栏两种形态（规格说明） | 900×340 | — | R2（对比说明） |
| 3:461 | 分类区块·响应式矩阵规格 | 940×340 | — | R2（对比说明） |
| 3:524 | 第二排标题区 | 1400×hug | — | R2（章节标题） |
| 3:527 | 首页·Medium 3×3 | 768×1024 | Medium | R3 |
| 3:592 | 首页·XL 5×2 | 1600×1000 | ExtraLarge | R3 |
| 3:657 | 我的·Expanded 宫格 | 1024×768 | Expanded | R3 |
| 3:811 | 发现·Compact | 393×852 | Compact | R3 |
| 3:889 | 视频详情·Compact 单栏 | 393×852 | Compact | R3 |
| 3:1036 | 设置·Large 双栏 | 1440×1000 | Large | R4 |
| 3:1118 | 设置·Compact 单栏 | 393×852 | Compact | R4 |
| 3:1154 | 第三排标题区 | 1700×hug | — | R4（章节标题） |

---

## 5. 跨页共享模式

### 5.1 顶栏变体（5 种）

| 形态 | 出现于 | 元素 | 备注 |
|---|---|---|---|
| **三件套** | 3:4 首页 Compact | 搜索胶囊（fill）+ 新番列表胶囊 + 头像 34dp | 中部顶栏也用于 3:527 Medium / 3:592 XL（但搜索/列表/头像顺序与宽度相应） |
| **标题 + 设置** | 3:5 我的 | "我的"标题 + 设置齿轮 | 标题居左，齿轮靠右 |
| **返回 + 搜索 + 取消** | 3:811 发现 | 返回 + 搜索框（fill）+ "取消" 文字按钮 | 中部是搜索场景专用 |
| **返回 + 操作组** | 3:889 视频详情, 3:1118 设置 | 返回 + （弹性占位）+ 右侧图标组 | 详情页：分享 / 下载 / 更多；设置页：空 |
| **返回 + 标题 + 顶栏动作** | 3:1036 设置 Large | 返回 + "设置" + "恢复默认" | 顶栏右部承担操作 |

### 5.2 一级导航变体（4 种）

| 形态 | 出现于 | 结构 | 高度 |
|---|---|---|---|
| **底部 NavigationBar**（Compact） | 3:4, 3:5, 3:811, 3:889 | 3 项：首页 / 发现 / 我的 | 80 |
| **Collapsed NavigationRail**（Medium/Expanded） | 3:6, 3:527, 3:657 | 80dp 宽；顶部展开切换 + 一级入口 + 弹性占位 + 底部设置 | fill_container |
| **Expanded NavigationRail**（Large/XL） | 3:7, 3:592 | 220dp 宽；品牌区 + 一级入口（pill 形）+ 弹性占位 + 设置（pill 形） | fill_container |
| **无导航条**（设置页全断点 + 视频详情 Compact） | 3:1036, 3:1118, 3:889 | 设置页是独立全屏路由；视频详情 Compact 是 `hidesNavigationChrome()` | — |

### 5.3 底部导航栏项内的元素（详见 §7.1 节点 3:70）

每个 tab 项是一个 vertical frame：图标（28–32 dp）+ label（caption 字阶）+ 选中态用
secondaryContainer 填色 + capsule 圆角。

### 5.4 NavigationRail 项内的元素

折叠态：仅图标。展开态：图标 + 文字横排（gap=12，padding=16），pill 圆角 28。

---

## 6. 章节标题区（横排注释，非屏幕）

### 6.1 3:1 第一排标题
- **主标题**：「LoveHan1me 跨平台布局原型 —— 导航形态与窗格重设计」
- **副标题**：一级入口收敛为 3 个：首页 / 搜索 / 资料库　·　Compact 用底部导航栏
  （设置里可切换悬浮胶囊形态）　·　Medium 与 Expanded 用折叠 NavigationRail
  　·　Large 起 2 窗格　·　tabletMode 开关取消，形态完全由可用宽度决定

### 6.2 3:524 第二排标题
- **主标题**：「补充断点覆盖 —— 把每个「页面 × 断点」都画出来」
- **副标题**：首页 Medium 3×3（竖屏平板 768×1024）· 首页 XL 5×2（桌面 1600×1000，
  展开 NavigationRail 220dp）· 发现页 Compact · 视频详情 Compact 单栏 · 我的
  Expanded 宫格（1024×768）

### 6.3 3:1154 第三排标题
- **主标题**：「设置页 —— 独立全屏、无 Rail 的响应式双栏」
- **副标题**：Large / XL：左栏分类导航 280dp + 右栏内容（限宽 840 居中）　·
  Compact / Medium：单栏分区滚动，顶栏只留返回 + 标题　·　任何断点都不显示 NavigationRail

---

## 7. 屏幕详细结构

### 7.1 3:4 · 手机·首页（Compact <600dp） — 393×1080

```
页面 (vertical, fill=#fdf8fa)
├─ 状态栏 (h=62, padding=24)
│   ├─ "9:41" 时间 (Inter SemiBold 15)
│   └─ 系统图标 (66×14)
├─ 顶栏 (h=64, gap=10, padding=16)
│   ├─ 搜索框胶囊 (fill×h=40, fill=#ecdedf, cornerRadius=20)
│   ├─ 新番列表胶囊 (hug×h=40, fill=#f5dde5, cornerRadius=20)
│   └─ 账号头像 (34×34, fill=#ffd9e2, cornerRadius=99)
├─ 内容区 (gap=24, padding=16)
│   ├─ Banner 精选 (fill×h=176, fill=GRADIENT_LINEAR [#583D8C→#ED739E 45°], cornerRadius=28)
│   └─ 分类·最新更新 (fill×hug)
└─ 底部导航栏 (h=80, fill=#ecdedf, padding=8, gap=4)
    ├─ 项·首页（选中，secondaryContainer 填色）
    ├─ 项·发现
    └─ 项·我的
```

### 7.2 3:5 · 手机·我的（Compact） — 393×852

```
页面 (vertical, fill=#fdf8fa)
├─ 状态栏 (h=62)
├─ 顶栏 (h=64, gap=18, padding=16)
│   ├─ "我的" 标题 (Noto Sans SC SemiBold 22)
│   └─ 设置图标 (22×22)
├─ 内容区 (gap=12, padding=16)
│   ├─ 签到首卡 (fill×hug, fill=#f6edef, cornerRadius=16)
│   ├─ 登录账户卡 (fill×hug, fill=#f5dde5, cornerRadius=20)
│   ├─ "我的内容" 分区标题 (Noto Sans SC SemiBold 15, color=#524344)
│   ├─ 行·收藏 (h=56, fill=#f1e1e5, cornerRadius=16)
│   ├─ 行·稍后看 (h=56, fill=#f1e1e5, cornerRadius=16)
│   ├─ 行·播放列表 (h=56, fill=#f1e1e5, cornerRadius=16)
│   ├─ 行·订阅 (h=56, fill=#f1e1e5, cornerRadius=16)
│   ├─ 行·观看历史 (h=56, fill=#f1e1e5, cornerRadius=16)
│   └─ 行·下载 (h=56, fill=#f1e1e5, cornerRadius=16)
└─ 底部导航栏 (h=80)
    ├─ 项·首页
    ├─ 项·发现
    └─ 项·我的（选中）
```

### 7.3 3:6 · 平板横屏·首页（Expanded 840–1199dp） — 1024×768

```
页面 (horizontal, fill=#fdf8fa)
├─ NavigationRail 折叠 80dp (w=80, fill=#ecdedf, padding=8)
│   ├─ 展开切换 (fill×h=48, cornerRadius=16)
│   ├─ 项·首页（选中，h=60）
│   ├─ 项·发现（h=60）
│   ├─ 项·我的（h=60）
│   ├─ 弹性占位 (fill×fill)
│   └─ 项·设置 (fill×h=56, cornerRadius=16)
└─ 主内容列 (fill×fill)
    ├─ 顶栏 (h=64, gap=18, padding=16)
    └─ 内容区 (gap=20, padding=16)
```

### 7.4 3:7 · 桌面宽屏·视频详情（Large 1200dp+） — 1440×1000

```
页面 (horizontal, fill=#fdf8fa)
├─ 主内容区 (flex=expand)
│   ├─ 顶栏 (h=64, gap=14, padding=20)
│   └─ 内容区 (gap=18, padding=24, top=8)
│       └─ ...（播放器 + 标题 + 操作 + tabs + 简介等元素）
└─ 侧栏·接下来播放 360dp (w=360, fill=#f6edef, padding=20)
    ├─ 侧栏标题行 (hug)
    ├─ 推荐·1 (hug)
    ├─ 推荐·2 (hug)
    ├─ 推荐·3 (hug)
    ├─ 推荐·4 (hug)
    ├─ 推荐·5 (hug)
    ├─ 推荐·6 (hug)
    ├─ 推荐·7 (hug)
    └─ 推荐·8 (hug)
```

### 7.5 3:527 · 首页·Medium 3×3（竖屏平板 768×1024） — 768×1024

结构同 3:6，但主内容区呈现 **3×3 分类网格**（按「可用内容宽度 656dp → 3 列」反推），
而非 3:6 的横向滚动。

### 7.6 3:592 · 首页·XL 5×2（桌面 1600×1000） — 1600×1000

```
页面 (horizontal, fill=#fdf8fa)
├─ NavigationRail 展开 220dp (w=220, fill=#ecdedf, padding=12)
│   ├─ 品牌区 (fill×h=56)
│   ├─ 项·首页（选中，fill×h=56, fill=#f5dde5, cornerRadius=28, padding=16, gap=12）
│   ├─ 项·发现（fill×h=56, fill=transparent, cornerRadius=28）
│   ├─ 项·我的（fill×h=56, fill=transparent, cornerRadius=28）
│   ├─ 弹性占位 (fill×fill)
│   └─ 项·设置 (fill×h=56, fill=transparent, cornerRadius=28)
└─ 主内容列 (fill×fill)
    ├─ 顶栏 (h=64, gap=18, padding=32 horizontal, 16 vertical)
    └─ 内容区 (gap=20, padding=32)
        └─ **5×2 分类网格**（按「可用内容宽度 ~1340dp → 5 列」反推）
```

### 7.7 3:657 · 我的·Expanded 宫格（1024×768） — 1024×768

结构与 3:6 同（Collapsed Rail + 主内容列），但「我的内容」6 个行项改为**宫格布局**
（每行 3–4 个 tile），Rail 的「我的」选中态。

### 7.8 3:811 · 发现·Compact（393×852） — 393×852

```
页面 (vertical, fill=#fdf8fa)
├─ 状态栏 (h=62)
├─ 顶栏 (h=64, gap=12, padding=16)
│   ├─ 返回 (22×22)
│   ├─ 搜索输入框 (fill×h=40, fill=#ecdedf, cornerRadius=20)
│   └─ "取消" (Noto Sans SC Medium 13, color=#9f4058)
├─ 内容区 (gap=24, padding=16)
│   ├─ 分区·搜索历史 (fill×hug, gap=12)
│   └─ 分区·热门搜索 (fill×hug, gap=12)
└─ 底部导航栏 (h=80)
    ├─ 项·首页
    ├─ 项·发现（选中）
    └─ 项·我的
```

### 7.9 3:889 · 视频详情·Compact 单栏（393×852） — 393×852

```
页面 (vertical, fill=#fdf8fa)
├─ 状态栏 (h=62)
├─ 顶栏 (h=56, gap=14, padding=16)
│   ├─ 返回 (22×22)
│   ├─ 弹性占位
│   ├─ 分享 (22×22)
│   ├─ 下载 (22×22)
│   └─ 更多 (22×22)
├─ 播放器 16:9 (fill×h=221, fill=GRADIENT_LINEAR [#293221→#5C382F])
│   ├─ 弹性占位
│   ├─ 播放按钮 (56×56, fill=#ffffff, cornerRadius=99)
│   ├─ 弹性占位
│   └─ 控制条 (hug, gap=10)
├─ 内容区 (gap=14, padding=16)
│   ├─ "【4K 修复】星野少女 第 01 话 · 完整版" 标题 (SemiBold 18)
│   ├─ 元信息行 (hug, gap=8)
│   ├─ 操作胶囊组 (hug, gap=8)
│   ├─ 页签行 (fill×h=40, gap=24)
│   └─ 简介正文 (Noto Sans SC Regular 12, color=#8a7278)
└─ 相关推荐 (gap=10, padding=16)
    ├─ "相关推荐" 分区名 (SemiBold 14)
    ├─ 推荐·1 (hug, gap=12)
    └─ 推荐·2 (hug, gap=12)
```

### 7.10 3:1036 · 设置·Large 双栏（1440×1000，无 Rail） — 1440×1000

```
页面 (vertical, fill=#fdf8fa)
├─ 顶栏 (h=64, gap=16, padding=24)
│   ├─ 返回 (22×22)
│   ├─ "设置" 标题 (SemiBold 22)
│   └─ "恢复默认" 顶栏动作 (Medium 13, color=#9f4058)
└─ 主体 (horizontal)
    ├─ 左栏·设置分类 280dp (w=280, fill=#f6edef, padding=16, gap=4)
    │   └─ 7 个分类项 (NavigationDrawerItem 形态)
    └─ 右栏·设置内容 (fill×fill, gap=20, padding=32)
        └─ 当前分类下的设置项（限宽 840 居中）
```

> **注意**：右侧内容**限宽 840dp 居中**（旧值，对应设计原型时期）——落地实现中已改为
> **640dp 居中**（表单档 `formMax`），正文档改为 720dp（`readingMax`）。详见
> 《跨平台响应式布局重设计方案》§4.3 落地记录。

### 7.11 3:1118 · 设置·Compact 单栏（393×852，无 Rail） — 393×852

```
页面 (vertical, fill=#fdf8fa)
├─ 状态栏 (h=62)
├─ 顶栏 (h=56, gap=16, padding=16)
│   ├─ 返回 (22×22)
│   └─ "设置" 标题 (SemiBold 20)
└─ 内容区 (gap=16, padding=16)
    ├─ 分组·播放 (fill×hug, fill=#f2ebee, cornerRadius=20)
    ├─ 分组·播放记录 (fill×hug, fill=#f2ebee, cornerRadius=20)
    └─ 分组·进阶设置 (fill×hug, fill=#f2ebee, cornerRadius=20)
```

---

## 8. 规格说明块（非屏幕）

### 8.1 3:8 · 导航栏两种形态（900×340）

> 「底部导航栏两种形态 —— 一级目的地：首页 / 发现 / 我的（设置项：导航栏样式）」

包含两个对比列：
- 列·**标准贴底**（393×hug）：标准 NavigationBar，矩形贴底，宽度 = 父容器宽
- 列·****悬浮胶囊**（393×hug）：Surface 胶囊形，距左右 16dp + 距底 16dp +
  `navigationBarsPadding()`；栏内 3 个 pill 项；bottomBar 同步留等高透明占位以免遮挡内容

### 8.2 3:461 · 分类区块·响应式矩阵规格（940×340）

> 「分类区块 · 响应式矩阵（列 × 行）」
> 「矩阵由「可用内容宽度」驱动，不绑定窗口断点 —— 常驻 Rail 会吃掉 80–220dp。
> 四档阈值按最小卡宽 170dp 反推得出。」

四档规格（横向并排）：
- **Compact**：2 列 × 4 行 = 8 卡 / 分类；间距 12dp
- **Medium**：3 列 × 3 行 = 9 卡；间距 16dp
- **Expanded**：4 列 × 2 行 = 8 卡；间距 16dp
- **Large / XL**：5 列 × 2 行 = 10 卡；间距 20dp

> 实现偏离：实际工程未按文档表的「5 列 → 6 列」对齐到 XL，而是在每个档位上**放大最小卡宽**
> （145 → 160 → 180 → 200 dp），由 `GridCells.Adaptive` 自动算列数。效果是「卡片更大」而非
> 「卡片更多」。

---

## 9. 与代码实现的对应关系

| 原型节点 | 实现位置 | 状态 |
|---|---|---|
| 3:4, 3:6, 3:527, 3:592 首页顶栏三件套 | `feature/home/homepage/component/HomeTopBar.kt` | ✅ 已落地 |
| 一级入口 3 个（首页 / 发现 / 我的） | `app/navigation/main/MainTab.kt` | ✅ 已落地 |
| Compact NavigationBar + Medium+ WideNavigationRail | `app/navigation/main/MainScaffold.kt` | ✅ 已落地 |
| 3:8 悬浮胶囊形态 | `MainScaffold.kt` + 动效 slow 档 | ✅ 已落地 |
| 3:7 视频详情双栏 + 360dp 侧栏 | `feature/video/VideoShellContent.kt` + `PageMetrics.relatedPaneWidthFor` | ✅ 已落地 |
| 3:7 视频播放器比例高度 | `PageMetrics.rememberWindowHeightDp()` + `VideoShellContent` | ✅ 已落地 |
| 3:1036, 3:1118 设置页双栏化 | `app/navigation/settings/SettingsTwoPaneHost.kt` | ✅ 已落地 |
| 内容限宽（表单 640 / 正文 720） | `ui/adaptive/ContentColumn.kt` + `HanimeDefaults.Widths` | ✅ 已落地（本批） |
| 3:889, 3:527 评论 / 简介接入正文档限宽 | — | ⏳ 待视觉确认（5 个接入点） |
| 3:5 我的页 6 个二级入口 | `app/navigation/main/MineSection.kt` | ✅ 已落地 |
| 设计系统 token（颜色 / 字阶 / 形状 / 动效） | `ui/theme/Defaults.kt` / `Shape.kt` / `Type.kt` / `Motion.kt` | ✅ 已收敛 |

---

## 10. 给后续 agent 的提示

1. **此原型只是意图对照，不要照抄**：原型的 2/3/4/5/6 列数表、840dp 限宽、固定的「卡片宽」等
   具体数值在落地时大多被有意调整过——以代码 `git log` 中的设计/实现提交为准。
2. **如果要做视觉回归**：跑 `desktopApp:run` 桌面端，覆盖 Compact / Medium / Expanded / Large
   四个窗口尺寸截屏对照本原型的 R1–R4 行。
3. **如果要做 token 校验**：跑全仓 `grep -n "HanimeDefaults\\."` 看 token 覆盖率，本原型里的
   颜色与字阶应与 `Defaults.kt` 对齐。
4. **如果要找组件引用**：组件源在 `shared/src/commonMain/kotlin/lovehan1me/ui/component/`，
   本原型未直接列组件 ID 但命名一致——可对照名字反查。

---

*本文档由 WorkBuddy 设计助手从 Ardot 画布 fileId `724724560492703` 静态抽取生成，
保留全部屏幕结构与设计 token，但不保留节点 ID 之外的几何精度（坐标 / 圆角细微差异）。
如需像素级精度，请直接打开原 Ardot 画布。*