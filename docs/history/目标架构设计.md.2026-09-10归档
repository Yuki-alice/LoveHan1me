# LoveHan1me 目标架构设计

> 制定日期：2026-09-10 ｜ 状态：v1（待评审）
> 输入：`E:\LoveHan1me` 现状审计 + 废案 `E:\hanime1` 结构拆解 + animeko 架构对标
> 定位：**独立开源项目**（非 fork），三端 KMP + Compose Multiplatform

---

## 一、身份标识（第一步要改的东西）

| 项 | 现状 | 目标 |
|---|---|---|
| 应用名 | `Han1meViewer`（rootProject.name） | **LoveHan1me** |
| 包名 / namespace | `me.lovehan1me` | **`io.github.lovehan1me`** |
| 原项目痕迹 | 作者 ID `daisukikaffuchino` 遍布全仓 | **全部清除** |

> ⚠️ **包名是"fork 感"最强的信号**。当前包名直接使用了原项目作者的 GitHub ID，这是最优先、成本最低、收益最高的一个动作。
> 若你的 GitHub 用户名不是 `lovehan1me`，告诉我，包名可一键替换（脚本化，成本极低）。

---

## 二、核心判断：架构优先，且「用架构重做功能」

### 2.1 为什么不能走「1:1 功能对齐」

原规划（v3）的阶段一「1:1 KMP 复刻」**与"不做 fork"的新约束直接冲突**：

- 逐页面搬运 → 代码结构、命名、实现细节都会高度雷同 → **相似度必然高**
- 结果是"KMP 版 Han1meViewer"，正是你不想要的东西

**结论：废弃「1:1 复刻」路线。**

### 2.2 正确做法

> **架构先行，但不停下来空做架构——而是让架构成为功能重写的容器。**

每写一个功能，同时完成三件事：① 落在新分层里 ② 用新命名体系 ③ 按新设计实现（而非搬运）。

**判断顺序**：
1. **先改身份标识**（包名）—— 零风险、立竿见影
2. **再固化包边界**（不拆模块，只在 `:shared` 内部分层）—— 零风险
3. **然后按模块重写**：数据层 → 领域层 → UI 层
4. **最后才拆物理模块**（仅在文件数足够多时）

---

## 三、废案结构拆解：学什么、丢什么

### 3.1 ✅ 值得学（4 条）

| # | 设计 | 废案位置 | 为什么值得 |
|---|---|---|---|
| 1 | **站点解析隔离**：`core:network` 只存**不透明字符串**，`SiteId`/`SiteConfig` 全在 `site/hanime1/SiteCatalog.kt`，新增站点时网络层零改动 | `site/hanime1/src/commonMain/kotlin/SiteCatalog.kt` | 这是**解析代理服务**（阶段二 P3）的基础；也让"站点语义"不下沉到基础设施 |
| 2 | **commonMain 契约 + jvmCommonMain 实现**：`OkHttp`/`Jsoup` 关进 `jvmCommonMain`，`commonMain` 保持纯接口 | `core/network/`（`commonMain` 只有 `NetworkContracts.kt`/`TransportContracts.kt`） | iOS 端能直接复用契约；平台依赖不扩散 |
| 3 | **字符串单源**：以 `androidMain/res/strings.xml` 为唯一源，用 `compose.resources { customDirectory(...) }` 生成跨平台 `Res.string` | `app-lang` 模块 | 避免维护两份资源，成本极低 |
| 4 | **纯文本扫描型检查**：如"`commonMain` 不得出现 `import java./javax./okhttp3./org.jsoup.`" | `site/hanime1/build.gradle.kts` 的 `verifySiteBoundaries` | 秒级、零插件依赖。**与用户反对的 21 个重型门禁不是一回事** |

### 3.2 ❌ 必须丢（3 条）

| # | 反模式 | 症状 | 教训 |
|---|---|---|---|
| 1 | **依赖三重包装** | `AppGraph`(59 构造参数) → 5 个 `*Runtime` → 7 个 Koin module，同一批依赖被包装 3~4 遍 | **不引入 Koin**，不造 `AppGraph`，依赖直接构造 |
| 2 | **feature 空壳化** | 5 个 `feature-*` 模块合计 **46 文件**，而 `app/shared/.../feature/` **100 文件** —— 同一功能横跨两模块 | **不为分层而分层**。要抽 feature 就抽完整的（Screen + ViewModel + DI） |
| 3 | **门禁塞进 Gradle** | 21 个 `verify*` 任务串进 `check`，每次全跑；甚至"在 build 脚本里写扫描逻辑来自测扫描逻辑" | 规则改动要改 Gradle 文件 → 拖编译、难维护 |

---

## 四、目标包结构（`:shared` 内部，Step 0）

不拆物理模块，先用**包边界**固化分层。零风险、可立即执行。

```
shared/src/commonMain/kotlin/io/github/lovehan1me/
├── core/                        # 平台无关能力（零 Compose、零 Android）
│   ├── domain/                  # 领域模型 + 仓储契约（纯 data class + interface）
│   ├── network/                 # 网络契约（不含 OkHttp 实现）
│   ├── storage/                 # 持久化契约
│   └── logging/                 # 日志契约
├── site/                        # ★ 站点解析隔离层（学废案）
│   ├── SiteId.kt                # 站点标识（不透明）
│   ├── SiteConfig.kt            # 站点配置（URL 构造、网络策略）
│   └── hanime1/                 # 单站实现
├── data/                        # 契约的实现（可含平台依赖）
│   ├── network/                 # Ktor 客户端、拦截器、Cookie
│   ├── parser/                  # HTML/JSON 解析（ksoup）
│   ├── database/                # Room
│   └── datastore/               # 设置存储
├── feature/                     # 按业务垂直切分
│   ├── home/  search/  detail/  player/  library/  account/  settings/
│   └── （每个 feature 内自带 ui/ + viewmodel/ + di，不跨模块）
└── ui/                          # 设计系统（阶段二 P2 的主战场）
    ├── theme/                   # 颜色/字号/圆角/间距 token
    ├── component/               # 通用组件
    └── adaptive/                # 断点与自适应
```

### 关键约束（写进规范，不进 Gradle）

1. `core/**` **不得** import `androidx.compose.*` 或平台 API
2. `site/**` 的 `commonMain` **不得** import `java./javax./okhttp3./org.jsoup.`（关进平台源集）
3. `feature/**` 之间**不得**互相依赖（要共享就下沉到 `core` 或 `ui`）
4. `ui/**` **不得**依赖 `data/**`（只依赖 `core/domain` 的契约）

---

## 五、渐进式模块演进（每步都能编译）

| Step | 动作 | 风险 | 理由 |
|---|---|---|---|
| **0** | **改包名 + 包边界固化**（本文档第四节的目录） | 低 | 零模块改动，先立身份、理顺方向 |
| **1** | 抽 `:core-logging`（+ 可选 `:app-lang`） | 低 | 依赖最少，用来试水约定插件是否顺手 |
| **2** | 抽 `:core-domain` / `:core-network` | 中 | **收益最大**：网络与领域从 UI 解耦 |
| **3** | 抽 `:site-*`（**仅当确认做多站**） | 中 | 支持代理服务与多站降级 |
| **4** | 抽 `:feature-X`（**仅当该 feature >100 文件且彼此零依赖**） | 中 | 避免废案的空壳化 |

> **不预设** `feature-detail` / `feature-player` 之类的空模块。

---

## 六、降低相似度的具体策略

| # | 策略 | 效果 | 成本 |
|---|---|---|---|
| 1 | **改包名** `daisukikaffuchino.han1meviewer` → `lovehan1me` | 极高 | 低（脚本） |
| 2 | **包结构重组**：`logic/` + `ui/screen/` 扁平结构 → `core/site/data/feature/ui` 分层 | 高 | 中 |
| 3 | **UI 层重写**：不搬运 composable 实现细节，按新设计系统重做 | **最高** | 高（本就是阶段二目标） |
| 4 | **删原项目特有功能**：伪装图标（4 个 `activity-alias`）、Getchu 预览、NDK 死代码 | 中 | 低 |
| 5 | **重写解析层**：`Parser.kt`(1194 行) 按 `site/hanime1/` 契约重写 | 高 | 中-高 |
| 6 | **命名体系统一**：`Hanime*` / `H*` 前缀（如 `HCacheManager`、`HDns`）改为语义化命名 | 中 | 中 |
| 7 | **移除原项目遗留文档**：`README_TECH.md`、`正式版构建行动指南.md` 等 | 中 | 低 |

---

## 七、合规（必须明确，非技术问题）

- 参考项目 `Han1meViewer-main` 主 LICENSE 为 **GPLv3**（附 Apache-2.0 兼容声明）
- **降低相似度 ≠ 规避 GPL**：若代码实质为派生作品，仍受 GPLv3 约束
- 两条路线：
  - **要 GPLv3/AGPLv3**：保留 `NOTICE` 归属（原作者 + Yenaly + MomoQR），诚实标注
  - **要 MIT/Apache**：必须真正重写，尤其 `data/parser` 与 `site/` 层不能是搬运
- **建议**：先按 GPLv3 走，等重写比例足够高后再评估；**不建议**在此事上冒险

---

## 八、立即执行清单

- [ ] **E1** 应用名 → `LoveHan1me`（`settings.gradle.kts`、`strings.xml`、`README`）
- [ ] **E2** 包名 → `io.github.lovehan1me`（全仓替换 + 目录搬迁 + `namespace`/`applicationId`）
- [ ] **E3** 包结构重组为第四节目录（`logic/`+`ui/screen/` → `core/site/data/feature/ui`）
- [ ] **E4** 清理原项目痕迹（`README_TECH.md`、`NOTICE` 收敛、死代码）
- [ ] **E5** 建立 `ARCHITECTURE.md`，写清第四节四条约束
