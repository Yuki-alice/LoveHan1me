# 取证记录格式

一份文件一个主题，文件名 `YYYY-MM-DD-<主题>.md`。日期 = 取证当天，不是写入当天。

    ## <一句话结论>
    - 取证日：YYYY-MM-DD
    - 重跑：<能原样复现这条结论的命令>
    - 环境：<机器/OS/版本/网络条件>
    - 失效条件：<什么变了这条就不成立>

`tools/check_doc_citations.py` 会检查四件套是否齐全、日期是否过期、`path:line` 是否还存在。
