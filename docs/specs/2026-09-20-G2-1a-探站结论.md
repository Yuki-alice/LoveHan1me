# G2-1a 探站结论（一页纸，2026-09-20；G2-1b-2 活体验证通过）

## 已验证的路由表（4 页实抓，夹具 `.workbuddy/g2b2_*.html`）

| 页面 | URL | 关键选择器 |
|---|---|---|
| 作者首页 | `/user/{id}` | `h1.profile-display-name`、`​.profile-avatar-wrapper img`、`.profile-sub-stats-new-line`（"N 位订阅者 • M 个视频"）、近期 `div.video-item-container` ×12、tab `/uploaded`、`/playlists` |
| 作者全部作品 | `/user/{id}/uploaded?page=N` | 同搜索页卡片（`hanimeNormalItemVer2` 复用）；`?page=2..8` 数字分页，单页 60 |
| 作者系列一览 | `/user/{id}/playlists` | 同构卡片，`a.video-link[href=/playlist?list=]` 取 listId，stat "N 部影片" |
| 独立系列页 | `/playlist?list=&sort=` | `h1.playlist-title`、`.playlist-author-info a[href*=/user/]`、`#sidebar-video-count`、`p.playlist-description`、`.filter-pill[.active]`（latest/popular/oldest）、条目 `.playlist-video-card`（内层同构卡） |

## comic 子站：可直接开工（2026-09-18 实测设计有效）

详见归档 `docs（不做参考）/漫画Tab设计.md`（hanimeone.me curl 直连实测），关键事实：
`/comics?page=N`（36 卡）/ `/comics/search?query=&page=` / `/tags/X` 等分类筛选 /
`/comic/{id}` 详情 / `/comic/{id}/{n}` 扁平阅读；封面读 `data-srcset`、阅读图读
`data-prefix`+`data-extension`（禁拼公式）；图片无防盗链、无 CF；三雷：MB 级索引页、
单作品 2600+ 页、抖动需重试。G2-1b 开工前只需一次时效抽查（需过盾网络，用 live test 或手动）。

## 视频侧作者页：有独立路由 `/user/{id}`（2026-09-20 更正，此前"无独立路由"是误判）

> 自我更正：初版据"解析层无 href + 上游无页 + 搜不到"断言无独立路由，实为样本不足
> 的误判（归档漫画文档点名过同类错误）。用户实证：`watch?v=408113` 的作者页为
> `/user/367299`。教训：以后"不存在"结论必须有正反两面证据。

* 链接链路现成：详情页 `video-subscribe-form` 的 `subscribe-artist-id`
  （`Parser.kt:621` → `HanimeVideo.Artist.POST.artistId`）即 id，无需新发现即可构造。
* 待探（需过盾网络）：`/user/{id}` 页结构（作者信息 + 作品列表 + 分页/排序？）。
* 作者列表：仍无"全部作者"路由实证，暂定 = 订阅作者（已解析）+ 搜索；若发现聚合路由再补。

## 系列页 playlist（用户新增范围，待确认是否进 G2-1b）

* 路由实证：`/playlist?list=976998&sort=latest`（清单 + 排序参数）。
* 存量：视频详情页已解析内嵌清单（`Parser.kt:444-471` `video-playlist-wrapper`，
  含清单名 + 视频卡），卡片解析可复用。
* 待探：独立 playlist 页结构（分页？sort 取值？）+ 清单归属（作者清单 vs 用户自建）。

## G2-1b 入口（2026-09-20 重排：comic 延后，先作者+系列）

1. 路由：新增 `ArtistRoute(userId)` + `SitePlaylistRoute(listId, sort)`（仿 `SearchRoute` 入栈）。
2. 作者：`/user/{id}` 解析（信息 + 作品列表 + 分页）+ 详情页 UI；id 取详情页既有 `artistId`；
   详情 `ArtistSection.onOpenArtist` 从 `openArtistSearch` 切到新路由（无 post 时回退搜索）。
3. 系列：独立 `/playlist` 解析 + 清单页 UI；详情页 `onShowAllPlaylist` 当前是空实现 `{}`，
   接到新路由（listId 若页内取不到，第一版用内嵌清单直接渲染全量页）。
4. 订阅页作者点击：订阅卡无 artistId，仍走搜索或补 id（待验证）。
5. comic：延后（归档设计保留，时效抽查同步延后）。
