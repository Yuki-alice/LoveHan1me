# Gate 1「收口」子任务提示词

> 用法：直接复制每节代码块发给执行 agent。所需 skills 已就位：
> `.qoder/skills/`（gradle-run、kotlin-api-design、kotlin-concurrency-and-flow、
> compose-expert、compose-ui-testing-patterns、grounded-writing）。
> 通用要求已写进每条：先加载 skill 再动手，跑 Gradle 前先过 `gradle-run` 规范。

---

## G1-1A `:app` 纯逻辑下沉 ✅ 已关闭（2026-09-20）

```
21 个 :app 文件已搬 shared/androidMain（同包同名，调用点零改）+ shared/build.gradle
补 documentfile/guava/work-runtime-ktx/livedata 四依赖；
ComposeActions 并入 ComposeActions.android.kt、VibrationUtil 并入 HapticFeedback.android.kt；
androidMain/res 资源一并迁移。shared 无反向依赖 :app（grep 零命中）。
```

## G1-1B 入口瘦身 + Worker 下沉 ✅ 已关闭（2026-09-20）

```
:app 剩 5 文件：HanimeApplication / BaseActivity / MainActivity / CrashActivity（纯入口壳）+
CrashHandler（崩溃入口接线，引用 :app CrashActivity，留 :app 是正确方向，shared 反向依赖它才是错）。
收尾动作：:app ActivityManager 删除，切到 shared restartApp + CurrentActivityHolder
（CrashActivity 重启入口、HanimeApplication.onActivityResumed 各一处），引用 grep 零命中。
DoD：compileAndroidMain + :app:compileDebugKotlin + assembleDebug + desktopApp + desktopTest 全绿 ✅。
```

## G1-2 compileSdk 收敛 ✅ 已关闭（2026-09-20，结论：统一单 37）

```
方向修正：原文“双平台 → 单 36”不可行，实测 26 个 AAR（Coil3 / lifecycle 2.11 /
material3 等）声明 minCompileSdk=37，降到 36 会在 :app:checkDebugAarMetadata
直接失败。收敛点改为单 37，工作区已达成，无需改值：
- gradle.properties：han1me.android.compileSdk=37（:shared）/
  han1me.android.appCompileSdk=37（:app），appTargetSdk 已降到 36
  （退出 Android 17 强制行为，与 compileSdk 解耦，需真机/模拟器实测边到边表现）。
- build-logic/.../han1me-kmp-library.gradle.kts:41 默认值已是 37。
- .github/workflows/ci.yml 已是单 37 安装 + 断言，无 android-36 字样。

wrapper 验证（2026-09-20，未改任何文件）：
- "Does the current single-37 compileSdk config compile Android plus Desktop?" → 通过（16s）
- "Does :app:assembleDebug pass on the single-37 config?" → 通过（15s）
- "Does :shared:desktopTest pass on the single-37 config?" → 通过（15s）
- "Does the iOS Kotlin Native compile pass?" → 初次失败（AppViewModelStore.kt:41,51
  `@Volatile` 无 import，kotlin.jvm.Volatile 在 Native 不可用），已修复：
  加 `import kotlin.concurrent.Volatile` 一行，iOS 双目标 61s 通过 ✅。

DoD：CI 无 android-36 字样；Android/Desktop 两链全绿。✅
剩余：targetSdk=36 真机实测（BaseActivity enableEdgeToEdge）随自用周覆盖。
```

## G1-3 死编号清理 ✅ 已关闭（2026-09-20）

```
基线从 33 处起算（11×TODO P5/P7 + 22×M6，含 2 处 base64 误命中），P6d 历史 90 处、P5.5 均保留。
改写：TODO P7→Gate3-平台能力；M6 契约→Gate2-站点纵深/Gate3；M6-2→删编号留功能注释。
例外：AppUpdateChecker.kt:44、UpdateJson.android.kt:19 的 "M6" 是 update URL base64
payload（cHM6），非死编号，不可改；DoD 第二条以 `grep -rn "M6\b"` 为准（零命中 ✅）。
第一条 `grep -rn "TODO.*P[57]"` 零命中 ✅。
wrapper 验证：compileAndroidMain + compileKotlinDesktop + :app:compileDebugKotlin +
:desktopApp:compileKotlin + :shared:desktopTest 全绿。
```

## G1-4 脚手架删除

## G1-4 脚手架删除 ✅ 已关闭（2026-09-20）

```
删 P3aVerificationScreen.kt（336 行，唯一引用是 DesktopScaffold.kt:23 注释）+
CookieSmoke.kt（79 行，唯一调用 Main.kt:89）及 Main.kt 接线 2 行；
DesktopScaffold.kt:23 去掉"P3a 保留"半句；IsDebugBuild.desktop（G1-3 已改）/
ios（补 Gate3 引用）只换注释。
留：DownloadSmoke.kt（端到端冒烟，非脚手架）、GenThemeBoards.kt（env 门控）、WindowStateStore.kt。
DoD：P3a/CookieSmoke 引用 grep 零命中 ✅ + :desktopApp:compileKotlin 17s 通过 ✅。
```

## G1-5 站点切换 + 双源弹幕收尾 ✅ 已关闭（2026-09-20，剩自用一周手动项）

```
测试（17 用例全绿，报告见 shared/build/test-results/desktopTest/）：
- SiteSwitcherToggleTest（desktopTest，4）：番剧→AV、AV→记住站、回程空兜底、回程指AV兜底。
- DanmakuMergeTest（commonTest，5）：乱序排序、位置过滤、双空、单源独撑×2。
- DanmakuStatusTest（commonTest，8）：优先级全分支。
生产抽取：remerge→mergeDanmakuItems、refreshStatus→resolveDanmakuStatus（internal 纯函数，行为零变）。
边缘收尾：DEF_VIDEO_TYPE/COVER_TYPE 上收 commonMain 唯一源（HanimeResolution/HanimeDownloadEntity
待回填注释关闭，androidMain 四处引用同步）；HanimeResolution TODO 清零。
wrapper：新测试过滤跑 + 全量 desktopTest + iOS 双目标全绿 ✅。
剩：自用一周（切站×镜像×登录态×弹幕）→ ROADMAP 勾选。
```

---

## skills ↔ 任务对照

| skill | 用途 | 任务 |
|---|---|---|
| gradle-run | Gradle 执行/诊断规范 | 全任务（编译验证） |
| kotlin-api-design | expect/actual、平台边界 | G1-1A、G1-1B |
| kotlin-concurrency-and-flow | 协程 scope、Flow | G1-1B、G1-5 |
| compose-expert | Compose 迁移/删除确认 | G1-1A、G1-1B、G1-4 |
| compose-ui-testing-patterns | 测试模式 | G1-5 |
| grounded-writing | 注释改写（证据写作） | G1-3 |
