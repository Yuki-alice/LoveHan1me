# E3 · 包结构重组执行方案

> 制定：2026-09-10 ｜ 状态：**待执行**（等编译验证能力建立后）
> 决策依据：用户选定「**单层项目前缀**」——包名 `lovehan1me.<layer>`，目录 `kotlin/lovehan1me/<layer>/`
> 目标：把原项目的 `logic/` + `ui/screen/` 扁平结构，重组为架构设计里的分层

---

## 一、为什么必须等编译验证

E3 与前两步（E1 改包名、E2 java→kotlin）**性质不同**：

| 步骤 | 操作 | 可否静态验证 |
|---|---|---|
| E1 包名替换 | 纯文本替换 + 目录搬迁 | ✅ 已用脚本验证（547 文件 0 错位） |
| E2 java→kotlin | 目录重命名 | ✅ 无内容改动 |
| **E3 结构重组** | **拆包 / 合并 / 重分组** | ❌ **会产生 import 断裂与潜在循环依赖** |

E3 必须**每改一批就编译一次**，否则错误会累积到无法定位。

---

## 二、现状 → 目标 完整映射

### 现状（commonMain，约 190 个直接文件 + 子目录）

```
me/lovehan1me/
├── App.kt  BasicConstants.kt  HanimeAccount.kt  HanimeManager.kt
├── HanimeResolution.kt  HanimeUrlRegex.kt  HorizontalCardCountConfig.kt
├── NetworkConstants.kt  Platform.kt  SearchGridColumnsConfig.kt      ← 根 10 个
├── logic/            15 文件
│   ├── dao/(12+download)     datastore/(3)    entity/(7+download)
│   ├── exception/(6)         model/(23)       network/(5+service)
│   ├── platform/(7)          state/(5)
├── ui/               
│   ├── adaptive/(2)  bridge/(1)  component/(25+appbar/content/lazy)
│   ├── crash/(1)     model/(2)   navigation/(0+main/settings)
│   ├── player/(9)    preview/(1) theme/(7)   viewmodel/(20+mylist)
│   └── screen/       2 文件 + account / crash / home(7 子包) / login
│                     / main / search / settings(2 子包) / video
└── util/(4)   utils/(10)
```

### 目标（顶层包 `lovehan1me`）

```
lovehan1me/
├── app/          组装根：App.kt、主容器、导航、崩溃兜底
├── core/         平台无关，禁止 import Compose / Android API
│   ├── domain/   领域模型、状态、错误、仓储契约
│   ├── platform/ 平台能力收口（expect/actual）
│   ├── constant/ 常量
│   └── util/     工具（合并原 util + utils）
├── site/         站点解析隔离（为代理服务铺路）
│   └── hanime1/
├── data/         契约实现，可含平台依赖
│   ├── network/  Ktor 客户端、拦截器、服务
│   ├── parser/   HTML 解析
│   ├── database/ Room DAO + Entity
│   └── datastore/
├── ui/           设计系统（阶段二 P2 主战场）
│   ├── theme/    颜色/字号/圆角/间距 token
│   ├── component/
│   ├── adaptive/
│   └── model/
└── feature/      业务垂直切分（每个 feature 自带 ui/ + viewmodel/）
    ├── home/  search/  detail/  player/  download/  library/
    └── account/  settings/
```

### 逐项映射

| 现路径 | 新路径 | 说明 |
|---|---|---|
| `/App.kt` | `app/App.kt` | 组装根 |
| `/Platform.kt` | `core/platform/Platform.kt` | 平台收口 |
| `/BasicConstants.kt` `/NetworkConstants.kt` | `core/constant/` | 常量归拢 |
| `/HanimeResolution.kt` `/HanimeUrlRegex.kt` | `site/hanime1/` | 站点专有 |
| `/HanimeAccount.kt` `/HanimeManager.kt` | `data/` 或 `core/domain/` | 视内容定（需逐个看） |
| `/HorizontalCardCountConfig.kt` `/SearchGridColumnsConfig.kt` | `ui/model/` | UI 配置 |
| `/logic/model/**` (23) | `core/domain/model/**` | 领域模型 |
| `/logic/state/**` (5) | `core/domain/state/**` | 状态模型 |
| `/logic/exception/**` (6) | `core/domain/error/**` | 错误模型 |
| `/logic/platform/**` (7) | `core/platform/**` | 与 `/Platform.kt` 合并 |
| `/logic/entity/**` (7+) | `data/database/entity/**` | Room 实体 |
| `/logic/dao/**` (12+) | `data/database/dao/**` | Room DAO |
| `/logic/network/**` (5+service) | `data/network/**` | 网络实现 |
| `/logic/datastore/**` (3) | `data/datastore/**` | 设置存储 |
| `/logic/*.kt`（Parser/GetchuParser 等 15） | `site/hanime1/` 或 `data/parser/` | 逐个判定：站点专有 → site |
| `/util/**` (4) + `/utils/**` (10) | `core/util/**` | **合并**，解决两个包并存问题 |
| `/ui/theme/**` (7) | `ui/theme/**` | 原地 |
| `/ui/component/**` (25+) | `ui/component/**` | 原地 |
| `/ui/adaptive/**` (2) | `ui/adaptive/**` | 原地 |
| `/ui/model/**` (2) | `ui/model/**` | 原地 |
| `/ui/player/**` (9) | `feature/player/**` | 播放器 |
| `/ui/screen/video/**` | `feature/detail/**` + `feature/player/**` | **需拆分**（详情 vs 播放） |
| `/ui/screen/home/**` (7 子包) | `feature/home/**`（download/subscription 可另立） | 见下 |
| `/ui/screen/home/download/**` | `feature/download/**` | 独立功能 |
| `/ui/screen/home/subscription/**` | `feature/library/subscription/**` | |
| `/ui/screen/search/**` | `feature/search/**` | |
| `/ui/screen/settings/**` (2 子包) | `feature/settings/**` | |
| `/ui/screen/account/**` + `/login/**` | `feature/account/**` | |
| `/ui/screen/crash/**` + `/ui/crash/**` | `app/crash/**` | 进程级兜底 |
| `/ui/screen/main/**` | `app/**` | 主容器 |
| `/ui/navigation/**` (main/settings) | `app/navigation/**` | |
| `/ui/viewmodel/**` (20+mylist) | 分散到 `feature/*/viewmodel/` | **需逐个判定归属** |
| `/ui/bridge/**` (1) `/ui/preview/**` (1) | `ui/` 或删 | 逐个判定 |

---

## 三、执行策略（**分批，每批一编译**）

绝不能一次性重排。建议按依赖方向从底层往上层推：

| 批次 | 内容 | 风险 | 验证方式 |
|---|---|---|---|
| **B1** | 顶层包改名 `me.lovehan1me` → `lovehan1me`（仅改前缀，不动结构） | 低 | 编译通过后提交 |
| **B2** | 合并 `util` + `utils` → `core/util` | 低 | 编译 |
| **B3** | 底层下沉：`logic/{model,state,exception}` → `core/domain`；`logic/platform` + `Platform.kt` → `core/platform` | 中 | 编译 |
| **B4** | 数据层：`logic/{dao,entity}` → `data/database`；`logic/network` → `data/network`；`logic/datastore` → `data/datastore` | 中 | 编译 |
| **B5** | 站点层：`Parser.kt` / `GetchuParser.kt` / `Hanime*` → `site/hanime1` | 中 | 编译 |
| **B6** | UI 层原地保留（theme/component/adaptive/model 不动） | 无 | — |
| **B7** | feature 拆分：`ui/screen/*` → `feature/*`；`ui/viewmodel` 分摊 | **高** | 每拆一个 feature 编译一次 |
| **B8** | 收尾：`app/` 组装根、导航、崩溃 | 中 | 编译 |

**B1 单独做**的理由：它只是前缀变化，与结构无关，先做能得到一个干净的起点；且和 E3 结构改动分离，出问题好定位。

---

## 四、自动化辅助

E3 无法纯脚本完成（涉及语义判断），但可脚本化其中两类：

1. **机械搬迁**：确定映射后，用 Python 脚本 `mv` 目录 + 替换 `package` / `import` 前缀（同 E1/E2 的做法，已验证可靠）
2. **一致性校验**：复用 `check_package.py`（校验 `package` 声明 vs 目录路径），每批后必跑

**剩余需人工判断的部分**：
- `ui/viewmodel/` 20+ 个 ViewModel 归属哪个 feature
- `logic/*.kt` 15 个文件哪些是站点专有（→ site）哪些是通用（→ data/parser）
- `HanimeAccount.kt` / `HanimeManager.kt` 的层次归属
- `ui/bridge/` / `ui/preview/` 是保留还是删

---

## 五、验收标准

- [ ] 最深目录从 6 层降到 ≤4 层
- [ ] `check_package.py` 报告 0 错位
- [ ] 三端编译通过
- [ ] `core/**` 无 `androidx.compose.*` import（用一次文本扫描确认）
- [ ] `feature/**` 之间无相互依赖
- [ ] `util` / `utils` 双包并存问题消除
