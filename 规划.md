# LoveHan1me 规划

> 唯一规划源。只管"现在什么样、下一步干什么"，不管历史、不设门禁。
> 做完一件事：把"当前焦点"换成下一个，顺手在复盘里记一行。
> 方向性的东西（是什么、给谁、赢在哪、先后顺序）看 [`定位.md`](定位.md)，这里不重复。

---

## 现状（2026-09-15）

- `shared/` 约 570 文件，分层 `core / site / data / feature / ui / app`，单模块不拆。
- `:app` 约 28 文件，壳 + 少量可下沉逻辑（HanimeCacheManager / HanimeFileManager / DownloadSettingsRoute 等还在里面，顺手再搬）。
- 包 ID `me.lovehan1me`，GPLv3。iOS 侧靠 CI 的 macOS 任务编译把关。
- 技术经验看 `.workbuddy/memory/MEMORY.md`。

## 当前焦点

**播放器性能优化**：目标超越网页体验。调研已完成（见 [`docs/视频播放优化.md`](docs/视频播放优化.md)），
第一步先补埋点立基线。不做：错误换源、倍速记忆、跳过片头尾。

## 待办池（顺序按定位：播放 > 离线 > 搜索 > 订阅 > 账号）

- 播放器继续打磨（手势、倍速记忆、错误换源、跳过片头尾）
- 离线下载（队列、断点续传、存储管理）
- 搜索发现补齐（排序语义、空态、历史管理）
- 订阅追番（更新检测、新集标出）
- 解析层重写（换宽松协议的前置，块大，单独排期）
- `:app` 瘦身成纯壳（顺手活，不专门排期）

焦点做完，从池子里挑下一个顺眼的，不预排死。

## 工作方式

- 小步提交到 main，信息写清做了什么。
- 提交信息只写完成了什么工作，不写阶段代号（M3a 之类已作废）。
- 推之前本地能编过就编，编不过靠 CI（Windows 编 Android+Desktop，macOS 编 iOS）。
- 不发版，只推 main。稳了再议发布。

## 复盘（做完记一行）

| 日期 | 做了什么 | 遗留 |
|---|---|---|
| 2026-09-15 | 文档重整：旧规划全归档，重写定位 + 规划 | 无 |
| 2026-09-15 | 账号链路桌面验证：新增 AccountChainTest（离线全通、live 探针）；CF 墙结论归档 | 真机 UI 走一遍 + 自有网络重跑 live（见当前焦点） |
| 2026-09-15 | 修登录两 bug（POST-302 误杀、CF 误报密码错），live 在本机全绿 | 无（等真机 UI 走一遍翻篇） |
| 2026-09-15 | 账号收尾翻篇（用户已验收）；播放器优化三路调研完成，文档入 `docs/视频播放优化.md` | 无 |

## 文档

根目录四份：`README.md`（对外介绍）、`定位.md`（方向）、`规划.md`（本文，下一步）、`Windows环境搭建说明.md`（环境坑）。其他全进 `docs/history/`。
