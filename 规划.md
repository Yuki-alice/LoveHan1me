# LoveHan1me 规划

> 唯一规划源。只写「现在是什么样、下一步想做什么」。
> 不设冻结清单、不设门禁、不要求子计划。做完一件事顺手更新几行即可。
> 旧规划全部在 `docs/history/` 吃灰，有需要再翻，不作为依据。

---

## 这是什么

hanime1.me 的第三方跨平台客户端，Kotlin Multiplatform + Compose Multiplatform，
Android / Desktop / iOS 一套代码。只做技术学习，不托管不分发任何内容。

## 现在是什么样（2026-09-14 实测）

- `shared/` 约 570 个 kt 文件，内部分层 `core / site / data / feature / ui / app`，单模块不拆分。
- `:app` 约 28 个文件，仍是"壳 + 少量可下沉逻辑"（HanimeCacheManager / HanimeFileManager /
  DownloadSettingsRoute 等还在里面，以后顺手再搬）。
- 包 ID `me.lovehan1me`，应用名 LoveHan1me，GPLv3。
- 最近在做的：播放器体验打磨、账号登录链路、冷启动埋点。iOS 侧代码在 Windows 上写、靠 CI 的 macOS 任务编译把关。
- 技术经验（源集规则、构建验证、踩过的坑）在 `.workbuddy/memory/MEMORY.md`，写代码前值得看一眼。

## 下一步（池子，挑顺眼的做，不排序）

- 账号链路收尾（登录/登出/过期跳转的真机走一遍）
- 播放器继续打磨（手势、倍速记忆、错误换源、跳过片头尾）
- 搜索 / 订阅 / 下载离线，缺什么补什么
- `site/hanime1` 解析层重写（想换许可证之前必须先做）
- `:app` 瘦身成纯壳（顺手活，不专门排期）

## 工作方式

- 小步提交到 main，信息写清楚做了什么。
- 推之前本地能编过就编，编不过就靠 CI（Android+Desktop 走 Windows runner，iOS 编译走 macOS runner）。
- 不发版，只推 main。什么时候觉得稳了再议发布。

## 文档

- 根目录只留三份：`README.md`（对外介绍）、`规划.md`（本文）、`Windows环境搭建说明.md`（环境坑）。
- everything else 进 `docs/history/`。
