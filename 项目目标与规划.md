# LoveHan1me 项目目标与规划 v2

> 制定日期：2026-09-10 ｜ 状态：**v2 草案，待评审**
> v1 → v2 变更：补入废案（`E:\hanime1`）教训、按用户新决策调整分期、新增「反 AI 堆砌」工程纪律
> 依据：`E:\LoveHan1me` 现状审计 + `Han1meViewer-main` 功能考古 + `animeko-main` 架构与差异化对标 + 废案 `docs/` 三份分析文档

---

## 0. 一句话目标

> 做一个**开源、三端（Android / Desktop / iOS）**的 hanime1 客户端，以**播放体验**为第一卖点；用自建**解析代理服务**把最脆弱的爬虫环节从 N 个客户端收敛到 1 个服务端；做到原网站**结构性做不到**的：离线观看、秒开首帧。

**本项目的特殊约束**：这不是从零起步，而是要**避免重蹈废案覆辙**。见 §2.5。

---

## 1. 现状诊断

### 1.1 硬数字

| 项 | 数值 |
|---|---|
| `shared/` 规模 | 506 kt / **54,815 行** |
| 源集分布 | commonMain 336 / jvmMain 28 / androidMain 46 / desktopMain 42 / iosMain 52 |
| `:app` 剩余 | 36 kt / 5,159 行（原参考项目 326 文件 / 52,602 行，约 90% 已下沉） |
| **构建验证次数** | **0**（无 build 产物、无 `.git`、无 CI、无测试、0 个 `@Preview`） |
| 参考项目 | 326 kt / 52,602 行，100% Compose，minSdk 29 |
| 废案 hanime1-client | 19 模块，147 commits，CMP 1.10.3，**已废弃** |
| 标杆 animeko | 69 模块 / 2,299 kt / 407,053 行 |

### 1.2 三层落差

| 层 | 完成度 | 说明 |
|---|---|---|
| **L1 跨端搬运** | ~85% | 网络/解析/数据/UI/导航/播放器抽象已进 commonMain |
| **L2 架构现代化** | ~15% | 只做了工具链，**领域架构的债原样搬进了 commonMain** |
| **L3 体验超越** | **0%** | 弹幕/推荐/云同步/秒开首帧，一项未做 |
| **地基** | **未验证** | 5.4 万行从未编译过一次 |

### 1.3 已搬进 commonMain 的架构债

| 债 | 现状 |
|---|---|
| God Object | `Parser.kt` 1,194 行（`hanimeVideoVer2` 单函数约 300 行） |
| 零 DI | **64 个 `object` 单例**，初始化顺序敏感，测试无法替换 |
| God Composable | `VideoPlayerUi` 单 composable 约 60 参数 |
| 数据库碎片 | **5 个独立 Room 库**未合并 |
| 解析脆弱 | 首页分区靠 DOM 下标 `getOrNull(0/1/2/3/5/6/7/8/10/11/12/13)`，站点改版即崩 |
| 双轨依赖 | Coil 3.4.0 vs 3.6.1、Nav3 1.1.5 vs 1.1.1、Jetpack vs CMP material3、`R` vs `Res` |

---

## 2. 已定决策

| # | 决策 | 选择 |
|---|---|---|
| D1 | 下一步 | **停下来先打赢编译战** |
| D2 | iOS 端 | **保留**（有 Mac） |
| D3 | 项目定位 | **开源社区项目**，GitHub 侧载分发 |
| D4 | 后端 | **做，先做解析代理服务** |
| D5 | 后端技术栈 | **Kotlin + Ktor** |
| D6 | 弹幕 | **做通用弹幕引擎 + 可挂载外部源** |
| D7 | 第一差异化卖点 | **播放体验做到极致** |
| D8 | 时间预算 | **每周 20h+，当主线干** |
| **D9** | **废案资产** | **仅弹幕可参考**；云同步、架构门禁、Koin、CI 等**暂不实现** |
| **D10** | **废案死因** | **AI 生成的代码堆砌，不可维护** |
| **D11** | 架构保障方式 | **不用编译期门禁**，靠规范与 review |

### 2.5 废案教训（死因：AI 代码堆砌，不可维护）

废案 `E:\hanime1` 的自我诊断，逐条对照新项目：

| 废案症状 | 新项目对照 | 风险 |
|---|---|---|
| `AppGraph.kt` 59 个构造参数、聚合 7 个 Runtime、`close()` 里 9 个 `runCatching` 静默吞异常 | 64 个 `object` 单例（同类问题，形态不同） | **高** |
| 巨型文件：`Hanime1HtmlParser` 66KB、`DownloadCenterScreen` 1660 行 | `Parser.kt` 1,194 行、`VideoPlayerUi` 60 参数 | **高** |
| 全项目 **0 个 `@Preview`**、Android 端验证空白 | 同样 0 `@Preview`，**且编译都没跑过** | **高** |
| 文档称某文件 1977 行，实测 52 行 —— **文档不可尽信** | 本规划每条结论均附文件路径 | 中 |

**由此得出本项目第一原则**：

> **写完 ≠ 跑通 ≠ 可维护。** 进度单位从"写了多少行"改为"**能跑、能演示、你真懂的有多少**"。

### 2.6 反「AI 堆砌」工程纪律（D10 的直接对策）

1. **停止扩写**：在现有 54k 行被验证并理解之前，不新增功能模块。
2. **理解度审计**（见 §7 待办）：逐模块确认"你看得懂、敢改"。
3. **每个改动必须当场验证**：编译 + 运行 + 演示，不允许"先写完后面统一调"。
4. **git 是底线，非可选项**：无版本控制时，AI 一次误改就无法回退。（D11 拒绝了编译期门禁，故此项不可再省）
5. **新代码必须带 `@Preview` 或测试**：废案 0 `@Preview` 是它失去可维护性的关键信号。

---

## 3. 差异化设计：凭什么超越原网站

### 3.1 关键判断：与 animeko **不同构**

animeko 的护城河是「bangumi.tv 只是元数据站，它把『查条目 → 看视频』那段路自己做完」。但 **hanime1.me 本身就能播**。

- ❌ 照抄 animeko 的 BT 下载、多源选源、字幕组偏好 → **无用功**（直链站点）
- ✅ 差异化是「**播得比它好**」+「**加它结构上做不到的东西**」

### 3.2 差异化矩阵

**A 类 · 原网站结构性做不到（护城河）**

| 功能 | 说明 | 依赖后端 |
|---|---|---|
| 离线下载 + 离线浏览 | 原站纯在线 | 否 |
| 播放器硬能力 | 手势族、快捷键、倍速记忆、画中画、后台播放、自动跳片头、Anime4K 超分 | 否 |
| 广告拦截 | 原站有广告 | 否 |
| 本地智能 | 基于本地观看行为的规则推荐、去重、统计 | 否 |

**B 类 · 体验碾压**

| 功能 | 目标 |
|---|---|
| 秒开首帧 | 预连接 + 解析缓存 + 地址预解析，目标 **< 1s** |
| 搜索 | 高级筛选、历史、即时搜索、本地索引 |
| 自愈能力 | Cloudflare / IP 封锁友好提示 + 自动恢复，**单源失败不拖垮全局** |

**C 类 · 从 animeko 抄的韧性设计**

- 选源**时间分段降级**：0–5s 精确 → 5–15s 放宽 → 15s 后模糊
- 播放错误**自动换源/降级**，截止时间压到 1 秒，保留黑名单
- 单源失败必须 emit 空结果，否则一个源炸了全局不发射

### 3.3 明确不做

- ❌ BT / P2P（直链站点，无必要）
- ❌ 真·个性化推荐（无数据规模）
- ❌ 社交 / 一起看
- ❌ 上架应用商店
- ❌ **云同步、编译期架构门禁**（D9/D11，暂不实现）
- ❌ **照搬废案除弹幕外的模块**

---

## 4. 架构目标

### 4.1 铁律

1. **平台 API 只出现在 `:platform`**，由 convention plugin 自动注入 commonMain
2. **三端启动器薄如纸** —— 启动器里出现业务代码即分层失败
3. **Page + ViewModel 同进 commonMain**
4. **依赖方向单向**：`platform ← data ← domain ← feature-ui ← application ← 启动器`
5. （D11）以上靠规范与 review 保证，**不引入编译期 verify 任务**

### 4.2 模块演进（渐进，不一步拆 45 个）

```
现状：:shared  :app  :desktopApp
  ↓ P1
:platform  :network  :parser  :database  :shared-ui  :app-shared
  ↓ P2
:source-api  :source-local  :source-remote
  ↓ P3+
:player-api  :player  :danmaku-api  :danmaku-ui  :feature-*
```

### 4.3 解析代理服务（D4/D5）

| 问题 | 客户端直爬 | 搬到服务端后 |
|---|---|---|
| Cloudflare 拦截 | 每个用户各自弹 WebView | 服务端统一处理，客户端无感 |
| IP 封锁 | 用户 IP 被封即不可用 | 服务端 IP 池 |
| DOM 改版 | **必须发版** | 服务端改一次，全网生效 |
| 解析逻辑泄露 | 客户端明文 | 收敛到服务端 |

---

## 5. 分期规划

> 按每周 20h+ 估算。每阶段独立可交付。

| 阶段 | 周期 | 内容 | 出口标准 |
|---|---|---|---|
| **P-1 环境** | 1 天 | `local.properties` / JDK 21 / platform / Gradle 9.4.1 | `:desktopApp:run` 跑起来 |
| **P0 编译攻坚** | 1–2 周 | 消灭双轨依赖、**纳入 git**、理解度审计 | 三端全绿；能说清每块代码在干嘛 |
| **P1 架构还债** | 2–3 周 | 拆 `Parser`、合并 5 个 Room 库、`res`→`composeResources`、DI 收口 | 双轨清零，模块数 ≥ 6 |
| **P2 解析代理 v1** | 3–4 周 | Ktor 服务端 + 客户端 A/B 切换 + 契约测试 | 站点改版无需发版 |
| **P3 播放体验** | 4–6 周 | 秒开、预加载、手势族、快捷键、自动跳片头、错误自动换源 | 首帧 < 1s |
| **P4 弹幕引擎** | 3–4 周 | 引擎 + 本地文件导入 + 外部源接口（**参考废案 `DandanplayDanmakuProvider` / `DanmakuHost`**） | 可挂载 ≥ 1 个源 |
| **P5 离线优先** | 3 周 | 下载 KMP 化（iOS/Desktop）、离线浏览 | 三端可离线观看 |
| **P6 发布** | 2 周 | README、Release 流程、下载页 | 首个公开 Release |

**总跨度约 5–6 个月。**（云同步已按 D9 移出规划）

---

## 6. 合规与风险登记

| # | 风险 | 等级 | 应对 |
|---|---|---|---|
| R1 | **GPLv3 传染**：参考项目主 LICENSE 是 GPLv3（附 Apache-2.0 兼容声明），本项目属派生作品 | 高 | 沿用 GPLv3 或 AGPLv3；NOTICE 保留原作者与 Yenaly / MomoQR 归属 |
| R2 | 成人内容 + 版权，开源分发存在法域差异 | 高 | 不托管媒体资源；README 声明"仅协议客户端"；不内置源配置 |
| R3 | 站点改版/关闭 = 项目死亡 | 高 | P2 代理服务 + 契约测试 + 多镜像降级 |
| **R4** | **重蹈废案覆辙：AI 代码堆砌不可维护**（D10 认定的死因） | **最高** | §2.6 五条纪律；停止扩写；理解度审计；git 底线；新代码带 `@Preview`/测试 |
| R5 | iOS 无法上架，只能侧载/TestFlight | 中 | 接受，README 说明 |
| R6 | 5.4 万行未验证代码的编译风险 | 中 | P0 专项攻坚 |
| R7 | 一人项目 + 研一课业与导师方向（ReID） | 中 | 每阶段可独立停下 |

---

## 7. 待定事项（需你拍板）

1. **项目名** —— `LoveHan1me` 是工作区名，是否作为发布名？
2. **许可证** —— GPLv3（继承，合规）还是 AGPLv3（与 animeko 一致）？
3. **理解度审计**（对应 R4，最高优先级）：54k 行里，你自己看得懂、敢改的占几成？**这是判断项目能否活下去的第一指标**，建议逐模块过一遍。
4. **灰色功能取舍** —— 伪装图标（4 个 `activity-alias`）、Getchu 预览等，开源后是否保留？
5. **后端部署** —— VPS / 家里云 / Serverless？
6. **是否保留客户端 Cloudflare WebView 验证** 作为代理服务的降级路径？
